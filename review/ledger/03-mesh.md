# Claim Ledger — `learning/03-peat-mesh.md` (peat-mesh module)

**Auditor:** adversarial doc reviewer · **Source of reality:** `learning/review/ground-truth/*.md`
**Target audited against:** peat-mesh GT @ `00ab0c9` (`0.9.0-rc.42`), with cross-checks to peat-lite GT @ `7a8a8fb` (`0.2.5`), peat-btle GT @ `3d70f48` (`0.4.0`), peat GT @ `35d0f11`.

Verdicts: **Verified · Outdated · Misleading · Oversimplified · Wrong · Unverifiable · Speculative-unlabeled**

| # | Claim (quote/paraphrase) | Location | Verdict | Evidence | Fix |
|---|---|---|---|---|---|
| 1 | "`peat-mesh` is the P2P networking library: pluggable transports, Automerge CRDT sync over QUIC, peer discovery, and topology formation." | §intro, L3-5 | **Verified** | GT peat-mesh §1 (Iroh/QUIC shipped), §2 (Automerge over Iroh shipped), §8 (discovery, topology shipped). | None. |
| 2 | "`peat-protocol` re-exports it, so an app developer rarely calls it directly." | §intro, L8-9 | **Verified** | GT peat §0: peat-protocol re-exports peat_mesh; security/network are thin `pub use peat_mesh::…` shims. | None. |
| 3 | `PeatMeshBuilder::new(MeshConfig::default()).with_transport(...).with_hierarchy(...).with_discovery(...).build()` then `mesh.start()` | §3.1(a), L20-27 | **Misleading** | GT peat-mesh §7: facade exposes `PeatMesh::new(config)` + `start()`; injectors are `set_transport`/`set_hierarchy`/`set_discovery` (`mesh.rs:224-456`), not `with_*`. README's `.build().await?` builder form is flagged as "aspirational/older" and "should be re-verified against `mesh.rs` before quoting verbatim." The `with_*` builder ergonomics and the `~line 575` cite are not corroborated. | Re-verify against `mesh.rs`; if the facade is `PeatMesh::new` + `set_*` injectors, show that form. Drop the unverified line number. |
| 4 | `MeshConfig` composes sub-configs: `topology`, `discovery`, `security`, `iroh`, `compaction`, optional `transport_manager`. | §3.1(a), L29-31 | **Unverifiable** | GT lists `MeshConfig` as a public type (peat-mesh §7) and confirms compaction/discovery/topology/security subsystems exist (§8), but does not enumerate the `MeshConfig` field set or cite `config.rs`. | Trace each named field to `src/config.rs`; flag any not present. |
| 5 | `peat-mesh-node` binary requires `--features node`; reads env, derives keys, picks discovery, gates Iroh, opens AutomergeStore, runs sync, broker, etc. (10-step list). | §3.1(b), L33-49 | **Unverifiable** | GT confirms the `broker` feature is an "Axum HTTP/WS service incl. OTA routes" (§7), mDNS + Kubernetes discovery (§8), and AES/ECDH crypto (§6). It does NOT confirm a `peat-mesh-node` binary, a `node` feature flag, `FormationPeerSet`, `AutomergeStore`/redb, `AutomergeSyncCoordinator`, `SyncChannelManager`, or `PeerConnector` by those names. | Open `src/bin/peat-mesh-node.rs` and the `node` feature in `Cargo.toml`; confirm each named type. Several may be aspirational. |
| 6 | Keys derived from `PEAT_FORMATION_SECRET` via **HKDF-SHA-256** (ADR-062). | §3.1(b) step 2, L40-41 | **Verified (mechanism); ADR# unverified** | GT peat §3: iroh `SecretKey` derived via HKDF-SHA-256 (v2 salt/info) since rc.14. GT peat-mesh §1 attributes Iroh/QUIC to ADR-062 Phase 2 (peat#926). HKDF-SHA-256 is FIPS-clean (peat GT §5). Deriving *from `PEAT_FORMATION_SECRET`* specifically is not confirmed in GT. | Keep HKDF-SHA-256; verify the env-var name and that ADR-062 is the right citation for key derivation. |
| 7 | "redb" is the on-disk backend for AutomergeStore. | §3.1(b) step 5, L44; §3.4 L141,145; §3.2 table | **Unverifiable** | GT peat-mesh confirms Automerge persistence and an `AutomergeStore`-class layer (§7-8) but never names **redb**. No GT citation for the storage engine. | Confirm `redb` dependency in `Cargo.toml` / `storage/automerge_store.rs`; cite or flag. |
| 8 | Source-layout table: `transport/`, `storage/`, `discovery/`, `topology/`, `routing/`, `security/`, `beacon/`, `qos/`, `broker/`, `network/`, `sync/`, `hierarchy/` with listed central types. | §3.2, L55-68 | **Partly Verified / partly Unverifiable** | GT confirms modules transport, storage, discovery, topology, beacon, qos, hierarchy, broker, and network/`IrohTransport` (peat-mesh §7-8, lib.rs re-exports). `NegentropySync` (§8), `TtlManager`/GC (§8), `BeaconJanitor`/`BeaconBroadcaster`/`BeaconObserver` not individually cited but plausible. `routing/` types (`MeshRouter`, `SelectiveRouter`, `DataPacket`), `sync/` `DocumentStore`/`SyncEngine`, `topology/` `TopologyBuilder`/`PeerSelector` are NOT confirmed by name in GT. | Spot-check the uncited type names against `src/`. The `DataSyncBackend` trait IS confirmed (peat-mesh §7) — good. |
| 9 | `security/` module does "P-256/AES-GCM encryption … X.509 enrollment" via `MeshCertificate`, `EnrollmentService`, `MeshGenesis`. | §3.2 table, L62 | **Partly Verified / Unverifiable** | AES-256-GCM + ECDH-P256 confirmed (peat-mesh §6). `DeviceKeypair`, `DeviceId`, `EncryptionKeypair`, `FormationKey` confirmed public (peat-mesh §6-7). `MeshCertificate`, `EnrollmentService`, `MeshGenesis`, and **X.509 enrollment** are NOT in GT for peat-mesh (membership-cert enrollment epic #592 lives in peat umbrella; `MeshGenesis` is a peat-btle type per peat-btle GT §3). | Verify X.509/enrollment types exist in peat-mesh vs. peat-protocol/peat-btle; relocate if misattributed. |
| 10 | discovery → connection flow: 3 strategies emit `DiscoveryEvent::PeerFound` → `PeerConnector` → `IrohTransport::connect` → `FormationPeerSet` gate (TLS + formation-key auth). | §3.3, L74-103 | **Partly Verified / Unverifiable** | GT confirms mDNS, Kubernetes, static, hybrid discovery (peat-mesh §8) and formation-key HMAC-SHA256 auth exists (§6; peat GT §7 `peat/formation-auth/1`). `DiscoveryEvent::PeerFound`, `PeerInfo`, `PeerConnector`, `FormationPeerSet` are not cited by name in GT. The mechanism is plausible and consistent. | Verify the named types/events; otherwise sound. |
| 11 | `SyncMessageType` enum with variants/bytes: DeltaSync 0x00, StateSnapshot 0x01, WindowedHistory 0x02, Tombstone 0x04, TombstoneBatch 0x05, TombstoneAck 0x06, SyncBatch 0x07, NegentropyInit 0x08, NegentropyResponse 0x09, NegentropyRequest 0x0A. | §3.4, L113-127 | **Unverifiable** | GT confirms `automerge_sync.rs` exists and uses the Automerge sync protocol over Iroh (peat-mesh §2, §8), and that negentropy is a separate module (`negentropy_sync.rs`, §8). It does NOT enumerate a `SyncMessageType` enum or these byte tags, nor cite `automerge_sync.rs:71`. Tombstones attributed to "ADR-034" — ADR-034 is not in any GT ADR index (peat-mesh §10 lists 0001-0013; peat GT §10 lists the umbrella set, no 034). | Trace the enum to `src/storage/automerge_sync.rs`; verify every byte value and the ADR-034 citation, or relabel the wire format Unverifiable. |
| 12 | "A naive Automerge sync can be O(n) in the number of documents. Negentropy … finds the difference … in O(log n) rounds by exchanging fingerprints (ADR-040)." | §3.4, L129-132 | **Oversimplified (cite OK)** | GT peat-mesh §8: negentropy module claims "O(log n) rounds, stateless sessions, 32-byte (SHA-256) doc IDs" but this is "algorithmic, not independently benchmarked here" (§9). ADR-040 + issue #435 correct (peat §2). The "naive Automerge = O(n) in #docs" framing is the doc's own characterization, not a sourced claim. | Keep ADR-040 cite; label "O(log n)" as the algorithm's claimed/analytical bound, not a measured PEAT result. |
| 13 | "After reconciliation, only the genuinely-missing docs/deltas are sent." / 6-step per-connection flow (negentropy → delta → token-bucket flow control → persist redb → QoS sync mode). | §3.4, L132-143 | **Partly Verified / Unverifiable** | Negentropy-before-delta and QoS sync modes (LatestOnly/FullHistory/Windowed) are consistent with GT (peat-mesh §2,§8). "token bucket per peer, cooldown on errors" and the exact ordering are not cited in GT. | Verify flow-control mechanism (token bucket) in `sync_channel.rs`. |
| 14 | Sequence diagram showing NegentropyInit/Response/Request then StateSnapshot/DeltaSync then TombstoneBatch/Ack, "O(log n) rounds", then "gossip … ADR-061". | §3.4, L151-166 | **Unverifiable / Speculative-unlabeled** | Same as #11 (byte tags unverified). The "ADR-061" gossip step is not supported anywhere in GT (see #15). | Gate the diagram on verifying #11 and #15; relabel ADR-061 content. |
| 15 | "Transitive gossip (ADR-061)": a node re-pushes a received remote change to every connected peer except the source; cites "DEVELOPER_GUIDE §6.4.1". | §3.4, L168-175 | **Speculative-unlabeled** | **ADR-061 appears in NO ground-truth file.** peat-mesh GT §10 ADR index lists only repo-local 0001-0013; peat GT §10 lists the umbrella ADR set (011/032/035/039/041/046/051/052/059/060/063/066) — no 061. Transitive-gossip fan-out behavior is not described in any GT. No `DEVELOPER_GUIDE` is cited in GT. | Verify ADR-061 and the DEVELOPER_GUIDE exist in-repo. If unverifiable, label the whole behavior **Speculative / unverified** explicitly per the §1 shipped-vs-proposed rule. |
| 16 | Bandwidth-envelope table: "~20% baseline"; LAN ≥256 kbps → N≤4 @2Hz or N≤7 @0.5Hz; BLE-class 30 kbps → N=3 only @0.5Hz; partial mesh diameter>3 out of envelope. | §3.4, L180-186 | **Speculative-unlabeled (quantitative)** | None of these figures (20%, 256 kbps, 30 kbps, N=3/4/7, 2 Hz/0.5 Hz, diameter>3) appear in any GT file. GT peat-mesh §9 explicitly lists the verifiable quantitative set and these are absent; BLE throughput "would live in peat-btle" and peat-btle GT §8 has no such envelope either. Presented as a firm "contract." | Mark every number Unverified/Speculative or trace to ADR-061 if it exists. Do not present as a "contract" without a source. High-risk: a skeptical engineer will ask where N=3 came from. |
| 17 | "lower `max_connections` (default 7)". | §3.4, L187 | **Unverifiable** | No GT citation for a `max_connections` default of 7. (Coincidentally, peat-btle's relay `DEFAULT_MAX_HOPS=7` and "N≤7" appear nearby — risk of conflation.) | Cite the default from `config.rs`/`topology` or flag. |
| 18 | Sync-cadence claim (ADR-063): coordinator advances one round per peer; each round opens a fresh QUIC stream; ~200 ms RTT caps throughput at "roughly 5 sync rounds per second per peer." | §3.4 "Why sync cadence…", L192-207 | **Partly Verified (ADR) / Unverifiable (numbers)** | ADR-063 ("Persistent Multiplexed Sync Streams") is real and **Proposed** (peat GT §10; peat-mesh GT §10 cites ADR-063/peat-mesh#175 at the rc.25 floor). So citing ADR-063 as a *proposal* is correct and honestly labeled. BUT the quantitative model (one-stream-per-message today, 200 ms RTT → 5 rounds/sec) is NOT in any GT. | Keep the ADR-063 proposal framing (good). Trace or flag the "5 rounds/sec" and one-stream-per-message figures; mark as analytical/unverified. |
| 19 | `pub struct NodeId(String);` shown as the transport identity type. | §3.5, L216-217 | **Verified** | GT peat-mesh §3: `transport::NodeId` is `pub struct NodeId(String)` (`src/transport/mod.rs:107`), a thin String newtype. | None. **Note:** this is the *correct* NodeId for peat-mesh — do NOT let other modules claim peat-mesh's NodeId is "SHA-256 of Ed25519 key." The crypto identity is `DeviceId = SHA-256(Ed25519 pubkey)[..16]` (truncated 16 bytes), surfaced via `From<DeviceId> for NodeId` (peat-mesh §3). Module text here is silent on the derivation, which is acceptable but could add the DeviceId nuance. |
| 20 | `ConnectionState` enum: Healthy (RTT<100ms, 0% loss), Degraded (RTT≥100ms OR loss>5%), Suspect (missed 2+ heartbeats), Dead. | §3.5, L219-225 | **Unverifiable** | GT peat-mesh §7 lists public transport types `ConnectionHealth` and `DisconnectReason` — but NOT a `ConnectionState` enum with these variants/thresholds. The specific thresholds (100ms, 5%, 2 heartbeats) are uncited. (Separately, GT §9 notes `TransportCapabilities::quic()` advertises 100 Mbps/10 ms as *hardcoded defaults*, cautioning against quoting hardcoded numbers as facts.) | Verify the type name (`ConnectionState` vs `ConnectionHealth`) and the threshold constants in `transport/mod.rs`; correct or flag. |
| 21 | "Three implementations plug into the same interface: Iroh QUIC, peat-lite UDP, Bluetooth LE." | §3.5, L228-234 | **Verified** | GT peat-mesh §1: exactly these three are shipped `MeshTransport` impls — Iroh/QUIC (`iroh_mesh.rs:508`), peat-lite UDP bridge (`lite.rs:841`, feature `lite-bridge`), BLE (`btle.rs:208`, feature `bluetooth`). Correctly omits SBD/LoRa (which are Proposed-only). | None. Good — this is the high-risk transport claim and the module gets it right (no SBD/LoRa as shipped). Could note BLE/lite are feature-gated. |
| 22 | Iroh QUIC "by default uses no relay servers … opt-in `relay-n0-hosted` feature enables n0's hosted relay pool." | §3.5 item 1, L230-232; §3.6 table | **Verified** | GT peat-mesh §1 "n0 relay posture": default `Endpoint::empty_builder`, no third-party relay/DNS; `relay-n0-hosted` off by default opts into `*.iroh.network`. Tactical-correct. peat GT §1 #833 tracks disabling n0 default. | None. (Could add the rc.42 runtime toggle `relay_policy_builder(enable_n0_relay)` per peat-mesh §1.) |
| 23 | peat-lite UDP transport "supports OTA." | §3.5 item 2, L233 | **Verified** | GT peat-mesh §8: OTA firmware push to Lite/ESP32, stop-and-wait, SHA256, optional Ed25519, `lite_ota.rs` (ADR-047), feature `lite-bridge`. | None. |
| 24 | Cross-transport bridging via `Translator` trait (ADR-059): a BLE document can be re-encoded and forwarded over QUIC and vice-versa. | §3.5, L236-238 | **Verified (with status nuance)** | GT peat-mesh §3,§8: `Translator` trait (`translator.rs`) + `BleTranslator` (`btle_translator.rs`), ADR-059, shipped under feature `bluetooth`. **Status nuance:** ADR-059 is **Proposed** (peat GT §10) and the trait *impl* moved INTO peat-mesh in peat-btle 0.4.0 (peat-btle GT §1). | Optionally note ADR-059 is formally Proposed though the codec/trait ship. |
| 25 | Feature-flag table: `automerge-backend` on by default; `bluetooth`/`broker`/`kubernetes`/`node`/`lite-bridge` off; `relay-n0-hosted` off. | §3.6, L244-252 | **Partly Verified / Unverifiable** | GT confirms `automerge-backend` default (peat-mesh §2), `bluetooth`/`lite-bridge`/`broker`/`kubernetes` features (§1,§7-8), `relay-n0-hosted` off-by-default (§1, `Cargo.toml:192`). The **`node`** meta-feature (= automerge-backend+broker+kubernetes+lite-bridge) is NOT confirmed in GT. "kubernetes pulls a FIPS crypto provider" — plausible (aws-lc-rs, §6) but uncited. | Verify the `node` feature composition and the kubernetes→FIPS-provider claim in `Cargo.toml`. |
| 26 | "Crypto/FIPS posture (ADR-060): the project moved toward FIPS-validated crypto — `aes-gcm` (AES-256-GCM), `p256` (ECDH P-256), and `rustls` backed by `aws-lc-rs` (replacing `ring`)." | §3.6, L254-256 | **Verified** | GT peat-mesh §6: ChaCha20/X25519 **removed**, replaced by AES-256-GCM (`aes-gcm 0.10`), ECDH-P256 (`p256 0.13`), HKDF-SHA256, aws-lc-rs TLS (not ring) per ADR-060 §5, dated 2026-05-18. This is the high-risk FIPS claim and the module is correct — no ChaCha20 propagated. | None. **Nuance:** wording "FIPS-validated" slightly overstates — algorithms are FIPS-approved; CMVP module validation is a separate in-flight concern (cf. peat-btle #75). Prefer "FIPS-approved primitives." Could also note residual ARM-perf concern (peat-mesh #126) is unbenchmarked. |
| 27 | Automerge sync ASCII diagram + "implements the Automerge sync protocol (paper arxiv 2012.00472) over Iroh." | §3.7, L264-279 | **Verified** | GT peat-mesh §2: Automerge sync protocol over Iroh QUIC streams, cites arxiv 2012.00472 (`automerge_sync.rs:1-25`). | None. (Verify the exact ASCII appears at top of file before claiming "the file's module doc draws the handshake.") |
| 28 | Leader election presented implicitly via `hierarchy/` "Static / dynamic (election) / hybrid hierarchy assignment". | §3.2 table L68 | **Verified** | GT peat-mesh §5: deterministic capability-scoring election (`dynamic_strategy.rs:190-224`), strategies Static/Dynamic/Hybrid (`hierarchy/mod.rs:10-12`), `NodeRole` Leader/Member/Standalone. No consensus. The high-risk "deterministic, no consensus" claim is consistent (though this module doesn't elaborate). | None for this module; the deterministic detail belongs to 02b. |
| 29 | Hierarchy vocabulary: `HierarchyLevel` referenced as a type without naming tiers. | §3.2 table L68 | **Verified (by omission) — caution** | GT peat-mesh §4: `HierarchyLevel = Node, Cell, Cohort, Federation, Coalition` — leaf is **`Node`**, NOT `Platform`. ADR-066's intended leaf is `Platform`; rename only partially landed in peat-mesh. This module does not assert tier names, so no violation here. | If any tier names are added later, present the leaf as **`Node`** for peat-mesh code and flag the pending `Node→Platform` rename (peat#904/#968). |
| 30 | QoS row: "5-level priority, sync-mode override, bandwidth, eviction/GC." | §3.2 table L64 | **Verified** | GT peat-mesh §8: QoS (bandwidth allocation, TTL, retention, sync modes, GC, preemption, eviction), `qos/*`, ADR-0013. Sync modes LatestOnly/FullHistory/Windowed (§8). "5-level priority" (`QoSClass`) plausible but the count "5" is uncited. | Verify the priority-level count "5" against `qos/`. Note QoS *enforcement* of "<5s P1" is in-flight per peat GT §9 (not asserted in this module — fine). |
</content>
</invoke>
