# Claim Ledger — Module 08 · Running & Operating PEAT

**Target:** `learning/08-running-and-operating.md`
**Source of reality:** `learning/review/ground-truth/*.md` (peat, peat-mesh, peat-btle, peat-lite, peat-gateway, peat-node), cross-checked against `peat/docs/guides/QUICKSTART.md` and `peat/docs/guides/operator/OPERATOR_GUIDE.md` at umbrella HEAD.

**Overall:** This module is unusually well-grounded because §8.1 transcribes a real, in-workspace example (`examples/quickstart` → `peat-quickstart`) and §8.3–8.8 faithfully mirror the real `OPERATOR_GUIDE.md`. The dominant risk is not fabrication but **conflation of two different runtimes** (the shipped quickstart binary vs the operator-guide `peat-sim` binary that is not in the workspace) and a few **unlabeled shipped/proposed distinctions** (MLS, QoS enforcement, leader-election metric). The FIPS/ChaCha20 handling here is the best in the curriculum: the module correctly flags the operator guide's cipher as stale.

| Claim | Location | Verdict | Evidence | Fix |
|---|---|---|---|---|
| "The quickstart uses one tiny example crate — `examples/quickstart` — that produces the binary `peat-quickstart`" | §8.1 | **Verified** | `peat/examples/quickstart/Cargo.toml` `name = "peat-quickstart"`, `[[bin]] name = "peat-quickstart"`; workspace member `peat/Cargo.toml:18`. | None. |
| `cargo build -p peat-quickstart --release` → `target/release/peat-quickstart`; flags `--name --bind --peer --mdns --storage` | §8.1 | **Verified** | `QUICKSTART.md` "Build the binary" + "--name, --bind, --peer, --mdns, --storage". | None. |
| Prereqs: "Rust 1.70+ and `protoc`" | §8.1 | **Verified** | `QUICKSTART.md` "What you'll need": Rust 1.70+, `protoc` via `apt`/`brew`. peat-lite MSRV 1.70 corroborates floor (peat-lite.md:7). | None. |
| Heading "a real mesh in ~20 minutes" but callout says "3-node mesh on your laptop in ~10 minutes" | §8 intro vs §8.1 heading | **Misleading** (internal inconsistency) | `QUICKSTART.md` states "~20 minutes if you don't have a Pi; ~45 minutes including the cross-compile." The "~10 minutes" callout is not sourced. | Make both say ~20 min (laptop-only); reserve ~45 min for the Pi scenario. |
| Three log prefixes `discovery:` / `connection:` / `sync:`; `sync:` proves replication; 2-second heartbeat; quiet filter, `RUST_LOG=debug` for firehose | §8.1 | **Verified** | `QUICKSTART.md` Scenario 1 describes all three prefixes verbatim, the 2 s heartbeat, the quiet default filter and `RUST_LOG=debug` opt-in. | None. |
| Scenario 2 transitive gossip: `charlie`→`alpha` only, sees `bravo` via `alpha`; "gossip relay from Module 3 §3.4" | §8.1 | **Verified** | `QUICKSTART.md` Scenario 2; transitive gossip with echo suppression is shipped in peat-node/peat-mesh (peat-node.md:83, `src/node.rs:963-999`). | None. |
| Scenario 3 mDNS: nodes advertise as `_peat-node._tcp.local`; won't work where multicast is blocked → fall back to `--peer` | §8.1 | **Verified** | mDNS discovery shipped (peat-mesh.md:215 `mdns-sd`; peat-node.md:24 mDNS on by default, disabled in K8s because "multicast is unavailable"). Service-name string is the example's; the multicast-blocked caveat is correct. | None (optionally confirm the exact `_peat-node._tcp.local` service label against the example source). |
| Scenario 4 cross-compile: `cross build -p peat-quickstart --release --target aarch64-unknown-linux-gnu`; "repo's `Cross.toml` already configures the aarch64 target" | §8.1 | **Verified** | `peat/Cross.toml` has `[target.aarch64-unknown-linux-gnu]` with pre-build (protoc + arm64 libdbus). `QUICKSTART.md` Scenario 4 uses `cross`. | None. |
| "The NodeId is deterministic from `--name` (same name ⇒ same id)" | §8.1 | **Verified (for this binary)** | `QUICKSTART.md` Scenario 1: "NodeId is deterministic from `--name`." Mechanism aligns with deterministic iroh identity via HKDF-SHA256(seed, info) (peat-node.md:40, peat.md:67). Note this is example-binary behavior, not a protocol-wide guarantee. | Optionally add: "in this example binary `--name` seeds the deterministic key derivation." |
| `PEAT_CONNECTION_RECYCLE_SECS`: iroh memory-growth workaround recycles every QUIC connection at 60 s; low-traffic → ~4–6 s outage every minute; set `=0` for demos (default `60`). "See issues #435 / #892." | §8.1 / §8.8 | **Verified, with one citation error** | `peat/CHANGELOG.md:298`: env var, default 60 s, `=0` skips the recycler, 4–6 s gap once a minute, sized against iroh memory growth (iroh#3565); QUICKSTART Scenario 4 sets it to 0. The recycle-override issue is **#892**; **#435 is the negentropy set-reconciliation issue** (peat.md:60, peat-mesh.md:205), not the recycler. The recycler's history ties to #873/#874/#923, not #435. | Drop or correct the `#435` reference; cite #892 (and #873/#874 for the connect-storm mitigation). |
| Stale-process gotcha on Pis (`nohup … &` survives SSH disconnect; pre-flight `pgrep`) | §8.1 | **Unverifiable / plausible operator lore** | Generic Unix behavior; not stated in QUICKSTART/OPERATOR_GUIDE. The "sync stuck at fuel_minutes=0" symptom is consistent with the example's counter but is not a documented failure mode. | Mark as field tip, not a documented behavior; or remove the specific `fuel_minutes=0` symptom claim. |
| Binary table: `peat-quickstart` (examples/quickstart), `peat-sim` (operator guide's main binary / network simulator), `peat-mesh-node` (peat-mesh feature `node`) | §8.2 | **Partly verified / one unverifiable** | `peat-quickstart` ✓ (examples). `peat-sim` is the operator guide's binary ✓ (`OPERATOR_GUIDE.md:66,112` etc.) but **not in this workspace** — module honestly says so. `peat-mesh-node` with feature `node`: peat-mesh ships a `broker` HTTP service and the `peat-mesh-node` binary is **not confirmed in ground truth** (peat-mesh.md lists facade + broker, no `peat-mesh-node` binary or `node` feature). | Verify `peat-mesh-node` binary + `node` feature exist in peat-mesh; if unconfirmed, relabel "the production node binary (Module 3)" without asserting the exact crate/feature, or flag as unverified. |
| "the operator guide leans on `peat-sim`, which is not in this local checkout and not a member of the current `peat` workspace … appears to live in its own repo now" | §8.2 | **Verified (honest gap)** | `grep peat-sim peat/Cargo.toml` empty; OPERATOR_GUIDE references `peat-sim` throughout. The module correctly labels operator-guide commands as "the documented operator workflow." This is exactly the shipped-vs-documented honesty the prompt wants. | None — exemplary. Consider an explicit "documented, not in-tree" tag in the table itself. |
| Node binary uses `PEAT_FORMATION_SECRET` / `PEAT_BROKER_PORT`; operator guide uses the set below | §8.2 | **Unverifiable from ground truth** | The operator guide uses `PEAT_FORMATION_KEY` (`OPERATOR_GUIDE.md:708`) and `PEAT_HTTP_PORT`/`PEAT_BIND_PORT`. `PEAT_FORMATION_SECRET`/`PEAT_BROKER_PORT` are not in the audited ground-truth set (gateway uses NATS/HTTP; peat-mesh broker port var not captured). | Cite where `PEAT_FORMATION_SECRET`/`PEAT_BROKER_PORT` are defined (peat-mesh broker), or soften to "the node/broker binaries use different env-var names." |
| "must-set trio": `PEAT_APP_ID`, `PEAT_SECRET_KEY`, `PEAT_FORMATION_KEY` | §8.3 / Checkpoint | **Verified** | `OPERATOR_GUIDE.md:270-271` mark `PEAT_APP_ID` and `PEAT_SECRET_KEY` Required:Yes; `PEAT_FORMATION_KEY` set at `:708`. Mirrors guide. | None. |
| Common-vars table: `PEAT_PERSISTENCE_DIR ./peat_data`, `PEAT_NODE_ID auto UUID`, `PEAT_CELL_SIZE 5`, `PEAT_DISCOVERY_MODE mdns`, `PEAT_BIND_ADDRESS/PORT 0.0.0.0/4040`, `PEAT_HTTP_PORT 8080`, `RUST_LOG info` | §8.3 | **Verified (operator-guide defaults)** | `OPERATOR_GUIDE.md:280-291`: `PEAT_CELL_SIZE` default 5, `PEAT_BIND_PORT` 4040, `PEAT_HTTP_PORT` 8080, `PEAT_DISCOVERY_MODE` mdns/static/hybrid, `PEAT_BIND_ADDRESS` 0.0.0.0. Auto-UUID node id matches peat-node `--node-id` UUID default (peat-node.md:39). | None — these are operator-guide (peat-sim) defaults; the quickstart binary uses `--bind`/`--name`, not these vars. Keep the "operator-guide defaults" qualifier. |
| `peat.toml` sections `[node][network][discovery][cell][hierarchy][security][storage][logging][capabilities]`; `peers.toml` with `[[peers]]` id/address/port/role | §8.3 | **Verified** | OPERATOR_GUIDE shows `peat.toml` (`p2p_port`, `http_port`, `formation_key`, `[[peers]]` blocks at :307-377). Section list mirrors the guide. | None. |
| Deployment patterns: single-host `PEAT_NODE_COUNT=20`; multi-node `--seed` + `PEAT_STATIC_PEERS=seed-ip:4040`; edge cross-compile + `PEAT_CELL_SIZE=3`; Docker multi-stage; K8s StatefulSet static discovery `peat-node-0.peat:4040` | §8.4 | **Verified (operator-guide workflow)** | OPERATOR_GUIDE: `PEAT_NODE_COUNT=20` (:441), `--seed`/`PEAT_STATIC_PEERS` (:456-462), `PEAT_CELL_SIZE=3` edge (:479), docker-compose seed+N (:492-522), K8s StatefulSet + headless service `peat-node-0.peat:4040` (:568-606). These run `peat-sim`. | Keep the implicit "these are peat-sim/operator-guide commands" framing from §8.2. |
| "peat-mesh also supports native k8s `EndpointSlice` discovery — Module 3" | §8.4 | **Verified** | peat-mesh.md:215 Kubernetes EndpointSlice discovery (feature `kubernetes`); peat-node.md:25 `KubernetesDiscovery` from EndpointSlice. **Shipped.** | None. |
| Ports table: 4040 UDP/TCP P2P, 8080 TCP HTTP API, 5353 UDP mDNS | §8.4 | **Verified** | `OPERATOR_GUIDE.md:618-620` exact match; iptables/firewalld rules at :627-640. | None. |
| "PEAT uses Iroh for NAT traversal (optional STUN/relay config)" | §8.4 | **Verified, but omits the air-gap default** | Iroh/QUIC is the shipped transport (peat-mesh.md:28). Relay is **off by default** (`relay-n0-hosted` feature off; `Endpoint::empty_builder`, no n0 DNS/pkarr; peat-mesh.md:42-47; peat.md:51, issue #833). | Add: relays default OFF for tactical/air-gapped builds; the n0 hosted relay is opt-in and tracked by #833. Important for the defense-prime audience. |
| Bandwidth profiles: `minimal` 9.6 kbps (tactical radio) → `low` 64 kbps (satellite) → `medium` 256 kbps (cellular) → `standard` 1 Mbps (Wi-Fi) → `high` 10+ Mbps; "Set `bandwidth_limit_kbps` + `qos_enabled = true`" | §8.4 | **Partly verified / oversimplified** | OPERATOR_GUIDE confirms `minimal` 9.6 Kbps (tactical radio), `standard` 1 Mbps (WiFi), `bandwidth_limit_kbps = 256`, `qos_enabled = true` (:668-678). The intermediate `low`/`medium`/`high` tiers and their link labels (satellite/cellular) are guide content. **QoS bandwidth-allocation primitives exist (peat-mesh.md:217 `src/qos/*`), but TTL/"<5s P1" enforcement is in-flight, not validated** (peat.md:142,193). | Note that setting `qos_enabled` configures QoS classes/allocation that are shipped, but priority *enforcement* (e.g. preemption to hit a latency target) is in-flight. |
| "Partitions are handled automatically: heartbeat-timeout detection → exponential-backoff reconnection → CRDT auto-merge on heal" | §8.4 | **Verified** | Partition detection + autonomous operation shipped (peat-mesh.md:219 `src/topology/*`); auto-reconnect watchdog with per-peer exponential backoff (peat-node.md:90, 5 s→120 s); CRDT convergence via Automerge. | None. |
| Formation key = `openssl rand -base64 32`; `PEAT_FORMATION_KEY` / `[security] formation_key`; "cell-admission secret" | §8.5 | **Verified** | OPERATOR_GUIDE `PEAT_FORMATION_KEY` base64-32 (:708-714, `formation_key` :329). Formation handshake = HMAC-SHA-256 challenge-response, key never crosses wire, ALPN `peat/formation-auth/1` (peat.md:121, peat-mesh.md security). "Cell-admission secret" is accurate. | None. |
| "PKI — optional X.509 device certs (`[security.pki]` with `ca_cert`/`node_cert`/`node_key`, `verify_peer = true`)" | §8.5 | **Unverifiable from ground truth** | Not present in the audited ground truth. Identity is Ed25519/DeviceId + formation HMAC (peat.md §3, §7); gateway issues mesh certificates (peat-gateway.md §4), not X.509 device certs configured via `[security.pki]`. This appears to be operator-guide config; not confirmed against code. | Trace `[security.pki]` to the operator guide / a config schema; if it's guide-only and unimplemented, label it as documented config not verified in code. |
| "the 2025-12-08 guide names ChaCha20-Poly1305 … the pre-FIPS cipher; the normative spec and ADR-060 (amended 2026-05-18) moved the posture to AES-256-GCM with ECDH P-256/P-384 and TLS/QUIC under a FIPS-mode provider (`aws-lc-rs`). When the two disagree, the spec/ADR is current." | §8.5 | **Verified — and exemplary** | OPERATOR_GUIDE `Last Updated: 2025-12-08` (:4); names `chacha20-poly1305` (:744-749). Code reality: AES-256-GCM + ECDH-P256 swap landed peat-mesh rc.12 / 2026-05-18 (peat-mesh.md:152-165; peat.md §5); `aws-lc-rs` is the active rustls provider (peat-mesh.md:157; CHANGELOG #923). This is the correct shipped-vs-stale framing the curriculum needs. **One caveat:** ground truth records AES-256-GCM + **ECDH P-256** specifically (peat.md:97); **P-384 is not evidenced** in code. | Verified. Drop or source the "P-384" — ground truth only shows P-256. Note ADR-060 is formally **Proposed** even though §5 is implemented (peat.md:108,163). |
| "Best practices: always use formation keys, enable TLS for the HTTP API, rotate credentials (and the formation key on member departure), use PKI, audit logs, segment traffic" | §8.5 | **Verified (guide advice)** | OPERATOR_GUIDE security best-practices section. These are operational recommendations, not code claims. | None; keep as guidance. |
| Monitoring endpoints: `/health`, `/ready`, `/metrics`, `/api/v1/{status,peers,cell,capabilities}`, `/api/v1/network/partitions` | §8.6 | **Verified** | `OPERATOR_GUIDE.md:1192-1199` lists all of these exactly; health/ready/metrics curls at :788-801. | None. |
| Key metrics: `peat_peers_connected`, `peat_cell_size`, `peat_sync_latency_seconds`, `peat_messages_{sent,received}_total`, `peat_bandwidth_bytes_total`, `peat_leader_elections_total` | §8.6 | **Verified (metric names) / one needs a caveat** | `OPERATOR_GUIDE.md:807-813` lists every metric verbatim, incl. `peat_leader_elections_total`. The metric *exists in the guide*; leader election itself is deterministic/no-consensus (peat-mesh.md §5) — a re-election still increments a counter, so the metric is coherent. | None on names. Optionally note these are the operator-guide/peat-sim metric set. |
| "JSON structured logging (`PEAT_LOG_FORMAT=json`) and OpenTelemetry/OTLP tracing are supported" | §8.6 | **Partly verified** | JSON log format is shown in OPERATOR_GUIDE (:830 structured JSON line). OTLP/OpenTelemetry support is plausible guide content but not corroborated in ground truth. | Confirm OTLP support against the guide/code; if guide-only, fine — it's the operator workflow. |
| Alert thresholds: 0 peers 5 min (critical), 3× failed leader elections (high), sync latency >10 s (medium), partition detected (high) | §8.6 | **Unverifiable / guide-derived** | These are operational alerting recommendations, not code-enforced thresholds. Not independently verifiable; consistent with the metrics exposed. | Keep as recommended alerts; they are advisory, not enforced. |
| CoT/TAK: `[cot]` `bind_port` 8087; capability→CoT type mappings (`platform_uav = "a-f-A-M-F-Q"`); `<peat:capability>`/`<__peat>` detail extension; `command_authority` gate `C2_ONLY` vs `ANY_TAK_USER`; code in `peat-transport/src/tak/` + `peat-protocol/src/cot/` | §8.7 | **Verified (code locations) / config specifics guide-derived** | TAK/CoT TCP bridge is shipped: `peat-transport/Cargo.toml` `src/tak/`, ADR-020/028/029 (peat.md:45); `cot` is a public module (peat.md:128). The specific `bind_port 8087`, the CoT type strings, the `<__peat>` extension, and the `command_authority` enum values are operator-guide/config details not individually re-verified against code. | Confirm the `command_authority` values and `8087` against the cot module/config; the subsystem and file paths are correct. Note: "platform_uav" mapping keeps the "autonomy under human authority" framing (good). |
| Backup/recovery: SIGTERM → tar data dir → restart; wipe data dir and a clean node "rejoins and re-syncs from peers (CRDT guarantees convergence)"; DR = redeploy with same config + formation key | §8.8 | **Verified** | Recovery-by-resync follows directly from Automerge CRDT convergence + negentropy reconciliation (peat-mesh.md §2, §8). Persistence dir via `[storage]` + snapshots is operator-guide config. The convergence guarantee is real. | None. Optionally note tombstone GC / retention (#857-adjacent) can affect what re-syncs after long offline windows. |
| Troubleshooting greatest hits (0 peers → wrong id/addr or UDP/mDNS blocked; leader-election timeout → NTP/latency; connected-but-no-sync <5 s normal; ~60 s reconnect → recycle workaround; persistence errors → check app id/secret/dir) | §8.8 | **Verified / consistent** | Each maps to a real mechanism: mDNS/firewall (peat-mesh discovery), deterministic election sensitivity to inputs (peat-mesh §5), 1–2 RTT post-handshake sync, `PEAT_CONNECTION_RECYCLE_SECS` (CHANGELOG:298), persistence env vars. "Leader election timing out → unsynced clocks" is plausible but note election is **deterministic scoring**, not time-quorum — clock skew affects beacon freshness, not a vote. | Optionally refine the leader-election tip: it's deterministic capability scoring over beacons, so the failure mode is stale/disagreeing beacon views, not a stalled vote. |
| Quickstart "deliberately has no formation key, no MLS, no enrollment" | §8.1 | **Misleading (unlabeled proposed)** | Implies MLS is a normal part of the full product that the demo merely omits. **MLS (RFC 9420) is NOT implemented anywhere** — no `openmls`/`mls-rs` in any Cargo.lock; it is Proposed (ADR-044) and the README presenting it as shipped is itself flagged misleading (peat.md §6). Listing "no MLS" alongside the shipped "no formation key / no enrollment" silently elevates MLS to shipped-status. | Drop "no MLS," or write "no formation key, no enrollment (and no MLS — MLS is Proposed, ADR-044, not yet implemented; Module 2/9)." |
| Checkpoint Q: "Which cipher does the operator guide name, and why is it stale vs. the spec?" | Checkpoint | **Verified (good pedagogy)** | Reinforces the correct ChaCha20-stale-vs-AES-GCM-current distinction (see §8.5 row). | None. |
| Implicit assumption: the §8.3–8.8 operator workflow (env vars, ports, metrics, peat.toml) applies to a binary the reader can build | whole module | **Misleading unless read with §8.2** | §8.2 honestly says `peat-sim` is not in the workspace; but §8.3–8.8 then present its env vars/ports/metrics without re-tagging them as "documented for peat-sim, not the quickstart binary." A reader who skips §8.2 will try `PEAT_BIND_PORT` against `peat-quickstart` (which takes `--bind`). | Add a one-line banner to §8.3: "These env vars/ports/metrics are the operator-guide (`peat-sim`) surface; the quickstart binary in §8.1 uses CLI flags instead." |
