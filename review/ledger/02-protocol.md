# Claim Ledger — `learning/02-peat-protocol.md` (Module 2: the `peat-protocol` facade)

Audited against the ground-truth files in `learning/review/ground-truth/` (peat HEAD `35d0f11`,
peat-mesh `00ab0c9`/rc.42, peat-btle `3d70f48`/0.4.0, peat-lite `7a8a8fb`/0.2.5) plus direct reads
of `peat/peat-protocol/src/` at the working-tree HEAD.

Verdicts: **Verified · Outdated · Misleading · Oversimplified · Wrong · Unverifiable · Speculative-unlabeled**

| # | Claim (quote/paraphrase) | Location | Verdict | Evidence | Fix |
|---|---|---|---|---|---|
| 1 | "`peat-protocol` is a *facade*… owns high-level concepts and re-exports lower layers (`peat-mesh`, `peat-schema`)" | §intro, lines 8-9, 19-22 | **Verified** | `peat.md:16,128` — peat is an umbrella; `peat-protocol/src/lib.rs:105-106` re-exports `peat_mesh`/`peat_schema`; consumers depend on peat-protocol alone. | None. |
| 2 | Module map: `cell`, `hierarchy`, `discovery`, `security`, `sync`, `qos`, `command`, `composition`, `cot`, `event`, `distribution`, `mesh_integration`, `policy`, `transport`/`network`, `geohash`, `traits`, `models` | §2.1 table, lines 27-45 | **Verified** | `peat.md:128` lists the public modules from `lib.rs:78-106`: `cell, command, composition, cot, credentials, discovery, distribution, error, event, ffi, geohash, hierarchy, mesh_integration, models, network, policy, qos, security, storage, sync, testing, traits, transport`. Doc table is a faithful (curated) subset; `transport`/`network` correctly flagged as re-export shims (peat.md:29). | Optional: note `storage` and `credentials` modules also exist (the doc references `storage::` in the example but omits it from the map). |
| 3 | `DEFAULT_CELL_SIZE = 5`, `DEFAULT_DISCOVERY_TIMEOUT_SECS = 60`, `DEFAULT_HIERARCHY_DEPTH = 4 // Node → Cell → Zone → Network` | §2.1, lines 50-53 | **Verified (constants) / Misleading (the depth comment's vocabulary)** | `peat-protocol/src/lib.rs:112,115,118` — all three constants match verbatim, and the source comment is literally `(node -> cell -> zone -> network)`. **But** "Zone → Network" is legacy vocabulary that conflicts with ADR-066 (Cell/Cohort/Federation/Coalition) which the same module cites at line 320. The doc faithfully copies the stale comment without flagging it. | Note that the `lib.rs` comment uses legacy "Zone/Network" terms; the hierarchy enum tiers are Node/Cell/Cohort/Federation/Coalition (ADR-066, Proposed). Flag the inconsistency rather than presenting it as canonical. |
| 4 | "there is **no `prelude` module** in current code — that snippet is aspirational" | §2.1, lines 64-66 | **Verified** | Correctly self-flags the `peat_protocol::prelude::*` import as illustrative. Honest "doc lags code" callout — exactly the kind of labeling the prompt wants. | None — this is a model for the rest of the doc. |
| 5 | Real example imports: `peat_protocol::network::{EndpointId, IrohTransport, PeerInfo}`, `peat_protocol::storage::{AutomergeBackend, AutomergeStore}`, `storage::capabilities::{CrdtCapable, SyncCapable, TypedCollection}` | §2.1, lines 69-73 | **Verified** | `peat.md:132` cites README entry points `IrohTransport::from_seed_at_addr`, `AutomergeBackend::with_transport`, `storage::capabilities::SyncCapable`. `AutomergeBackend` exists at `src/storage/automerge_backend.rs:118` (and a second at `src/sync/automerge.rs:49`). | None. |
| 6 | Three-phase flow: Discovery → Cell Formation → Hierarchical Ops, with "leader lost → re-elect" and "timeout/too few → discovery" loop-backs | §"three phases", lines 82-88 | **Verified (pedagogical model)** | Consistent with `peat.md:120` (re-election triggers in `hierarchy/maintenance.rs:227/252/312`) and the discovery→formation→ops staging. The three-phase framing is the doc's own teaching scaffold, reasonable. | None. |
| 7 | Discovery strategies: `peer.rs`, `geographic.rs`/`geo.rs`, `directed.rs`, `capability_query.rs`, `coordinator.rs`; "actual radio/transport discovery lives in peat-mesh" | §2.2, lines 95-104 | **Verified** | `peat.md:27` — peat-discovery removed (peat#919); discovery now in `peat_mesh::discovery`. Doc correctly says transport-level discovery (mDNS sockets, k8s) is one layer down. peat-mesh.md:215 confirms mDNS/static/k8s/hybrid discovery shipped in peat-mesh. | None. File-name list is plausible but not independently re-verified at line level — low risk. |
| 8 | "Leader election is deterministic… every node computes the same score… ties broken lexicographically by node id" | §2.3, lines 121-125 | **Verified** | peat-mesh.md:122-133 — `DynamicHierarchyStrategy::determine_role` is deterministic capability scoring, NO Raft/Paxos/quorum; each node independently computes the same ordering. peat-protocol's `cell/leader_election.rs` mirrors this in the domain layer. **Shipped.** | None — this is the single most important high-risk claim and it is correct. Add a "Shipped" label per the prompt's labeling rule. |
| 9 | Leader-election weights: `compute*0.30 + communication*0.25 + sensors*0.20 + power*0.15 + reliability*0.10` | §2.3, lines 127-134 | **Verified** | Direct read: `peat-protocol/src/cell/leader_election.rs:101-106` — exact weights `0.30/0.25/0.20/0.15/0.10`; comment at `:101` matches. Each sensor adds 0.25 capped at 1.0 (`:92,98`). | None. (Note: this is the *peat-protocol* domain election; the *wire-level* score in peat-mesh `dynamic_strategy.rs:100-138` uses a different mobility/resource/battery formula — two distinct scoring functions. Optional: clarify which layer's election this snippet is.) |
| 10 | `CellCoordinator::check_formation_complete()` validates min size, leader elected, all members have roles, capability coverage, readiness ≥ 0.7, and human approval for mission-critical | §2.3, lines 136-141 | **Verified** | Direct read: `src/cell/coordinator.rs:14` (readiness threshold default 0.7), `:53,77` (`min_readiness: 0.7`), `:38-39` (`AwaitingApproval`), `:97,140-144` (readiness check), `:22` (human approval if mission-critical capabilities lack DirectControl). | None. |
| 11 | Composition rules: additive / emergent / redundant / constraint, with ISR emergent example (camera+comms+range) | §2.3, lines 145-152 | **Verified (mechanism) / Oversimplified (the specific rule set)** | `src/composition/additive.rs` and `engine.rs:151` confirm additive + `CapabilityType::Emergent` synthesis. The four-rule taxonomy (additive/emergent/redundant/constraint) is a reasonable teaching summary; "redundant"/"constraint" as named rule kinds were not line-verified in this pass. | Low priority: confirm "redundant" and "constraint" exist as discrete rule types vs. teaching shorthand; label accordingly. |
| 12 | Capability composition table: `Sensor`→additive, `Compute`→additive, `Communication`→aggregated, `Payload`→additive, `Mobility`→constraint, **`Weapon`→not composed** | §2.3, lines 157-164 | **Wrong** (the `Weapon` row) | There is **no `Weapon` CapabilityType.** The enum is `UNSPECIFIED, SENSOR, COMPUTE, COMMUNICATION, MOBILITY, PAYLOAD, EMERGENT` (`peat-schema/proto/capability.proto:11-19`); `PAYLOAD` is commented "Payload/weapon capabilities." So "Weapon" is not a distinct capability type. The doc invents a seventh row. | Remove the `Weapon` row, or relabel it `Payload` (which the proto explicitly covers weapons under). Add the real `Emergent` type (output of composition) to the table or note it's synthesis-only. |
| 13 | Cell roles: `Leader` (one per cell), plus `Sensor`, `Compute`, `Relay`, `Strike`, `Support`, `Follower` | §2.3, lines 166-168 | **Verified** | Direct read: `src/models/role.rs:14-28` — `CellRole { Leader, Sensor, Compute, Relay, Strike, Support, Follower }` (7 variants, exactly as listed); `:32` notes Leader is elected not assigned. **Note:** ground-truth `peat.md:84` omitted `Support` from its CellRole list — the doc is actually MORE accurate than that ground-truth note here; code confirms `Support` exists (`role.rs:25`). | None. (The doc must not conflate this `CellRole` capability axis with the RBAC `Role` enum — it correctly does not here; see #21.) |
| 14 | Hierarchy files: `aggregation_coordinator.rs` (`HierarchicalAggregator`), `state_aggregation.rs`, `router.rs`+`routing_table.rs`, `flow_control.rs`, `deltas.rs` (`CellDelta`/`CohortDelta`/`FederationDelta`/`CoalitionDelta`), `storage_trait.rs` | §2.4, lines 176-182 | **Verified** | Delta names use the ADR-066 tiers (Cell/Cohort/Federation/Coalition), consistent with `peat-protocol`'s `authorization.rs:331-343` already-migrated `HierarchyLevel` (peat.md:76). `HierarchicalAggregator` struct quoted at line 203 matches the described create-once/update-many pattern. | None. |
| 15 | Routing rule: same cell → allowed; cross-cell direct → rejected (must route up through leader) | §2.4, lines 186-197 | **Verified** | peat.md:122 — `hierarchy/router.rs:19-20,90-91,140`: leaders route upward; non-leaders cannot cross-cell or reach zone. Matches the quoted `router.rs:~86` snippet. **Shipped.** | None. |
| 16 | State flows up (node → cell → cohort → federation → coalition); commands flow down; peers handle handoffs horizontally | §2.4, lines 195-198 | **Verified** | Consistent with ADR-066 tier chain and `HierarchyLevel.parent()/child()` (peat-mesh.md:104-107). | None. |
| 17 | `AutomergeBackend` struct fields: `documents: HashMap<String, Automerge>`, `sync_states`, `observers`, `tombstones (ADR-034)`, `lamport_counter (deterministic ordering)` | §2.5, lines 228-237 | **Verified** | Direct read: `src/sync/automerge.rs:65-72,98` — `tombstones: Arc<Mutex<HashMap<String, Tombstone>>>` (ADR-034), `lamport_counter: Arc<AtomicU64>` ("Monotonic Lamport counter for tombstone ordering, ADR-034, Issue #668"). Matches the quoted struct. | None. (Minor: the quoted snippet cites `~line 44`; actual struct is at `:49` — cosmetic.) |
| 18 | "CRDT documents grouped into named collections… merge automatically… operations succeed locally first (offline-first)" | §2.5, lines 215-218 | **Verified** | peat-mesh.md:55-59 — Automerge documents over Iroh; CRDT semantics are Automerge's; offline-first/local convergence is the design. **Shipped.** | None. |
| 19 | "actual wire sync (negentropy reconciliation, Iroh streams, redb persistence) happens in peat-mesh" | §2.5, lines 240-241 | **Verified** | peat-mesh.md:205-208 — negentropy set reconciliation (`storage/negentropy_sync.rs`, ADR-040 #435) + Automerge sync over Iroh QUIC, both **Shipped**. peat.md:60 confirms `NegentropySync` re-exported. Correctly attributes wire sync to peat-mesh, not peat-protocol. | None — note this is the correct framing: anti-entropy IS implemented (negentropy), NOT the speculative version-vector/IBLT schemes. |
| 20 | "Which CRDT for which data" table: capabilities→G-Set, membership→OR-Set, leader→LWW-Register, position→LWW-Register, fuel→PN-Counter, history→G-Set, config→LWW-Map | §2.5, lines 248-256 | **Misleading / Oversimplified** | This is a **design-pattern teaching table, not the shipped reality.** The shipped core CRDT engine is **Automerge** (a single document-CRDT covering maps/text/lists — peat-mesh.md:55-59); it does NOT expose typed G-Set/OR-Set/PN-Counter/LWW-Map primitives per data type. The named primitives (`LwwRegister/GCounter/PnCounter/OrSet`) live ONLY in `peat-lite` (peat-lite.md:50-58) — and even there `PnCounter` is firmware-only and `OrSet` is a reserved wire byte with no implementation. `peat-protocol` itself stores everything as Automerge docs. A reader will wrongly infer peat-protocol picks a discrete CRDT type per collection. | Relabel as "conceptual mapping / design intent." State explicitly: the runtime uses Automerge documents (one CRDT family), not these discrete typed CRDTs; the named primitives are peat-lite's embedded-tier types, not peat-protocol's. Mark **Speculative/illustrative** for the per-type mapping. |
| 21 | "only one [backend] ships today: Automerge + Iroh (100% OSS). An earlier Ditto-SDK backend was removed (ADR-011). Treat 'Ditto' in older docs as historical." | §2.5, lines 258-261 | **Verified** | peat-mesh.md:55, peat.md:154 (ADR-011 CRDT+Networking, Ditto vs Automerge/Iroh — **Proposed** status, but Automerge/Iroh is the shipped choice). Honest historical flag. | Minor: ADR-011 is formally **Proposed** (peat.md:154), even though the Automerge+Iroh decision it documents is shipped — a code-ahead-of-ADR case worth a one-line note. |
| 22 | Five QoS classes with latency targets and bandwidth shares: P1 ~500ms/40%, P2 ~5s/30%, P3 ~60s/20%, P4 ~300s/8%, P5 no limit/2% | §2.6, lines 270-280 | **Verified (figures) / Oversimplified (enforcement status)** | Direct read: `src/qos/mod.rs:41-45` — verbatim table (500ms/40%, 5s/30%, 60s/20%, 300s/8%, None/2%); latencies confirmed in `src/qos/classification.rs:131,165,199,233,267`. **BUT** these are *configured targets*, not enforced/validated SLAs. peat.md:142,193 — QoS enforcement (TTL/bandwidth, "<5s P1") is **in-flight**, not validated; peat-mesh.md:218 lists QoS framework shipped but bandwidth/latency are advertised defaults. | Add: the bandwidth/latency figures are **configured policy targets** (`QoSRegistry::default_military()`), not measured or enforced end-to-end; P1 "<5s" enforcement is in-flight. Label **Shipped (framework) / In-flight (enforcement)**. |
| 23 | "`QoSRegistry::default_military()` ships sensible defaults… oldest/lowest-QoS evicted first (`qos/eviction.rs`, `qos/garbage_collection.rs`)" | §2.6, lines 278-280 | **Verified** | `src/qos/mod.rs` exports BandwidthAllocation/Quota machinery; eviction + GC modules referenced in peat.md:193, peat-mesh.md:218. **Shipped.** | None. |
| 24 | Security: Ed25519 device identity + challenge-response (`authenticator.rs`, `device_id.rs`) | §2.7, lines 288-289 | **Verified** | peat.md:95 (Ed25519, FIPS 186-5), peat.md:66 (`device_id.rs` re-export, deterministic `from_public_key`). **Shipped.** | None. |
| 25 | Formation keys: pre-shared secret, HKDF-derived `FormationKey` + HMAC-SHA-256 challenge/response over a dedicated ALPN | §2.7, lines 290-293 | **Verified** | peat.md:99,121 — `FORMATION_HANDSHAKE_ALPN = b"peat/formation-auth/1"`, HMAC-SHA-256, 30s timeout, key never crosses the wire, `subtle` constant-time compare. peat-mesh.md:155 (`formation_key.rs`). **Shipped.** | None. |
| 26 | "Encryption — AES-256-GCM AEAD with ECDH key exchange; the project is moving toward a FIPS 140-3 posture (ADR-060)" | §2.7, lines 296-297 | **Verified** | peat.md:96-97 — AES-256-GCM (SP 800-38D) + ECDH-P256 (SP 800-56A, swapped from X25519 rc.12); peat-mesh.md:145-155. **Critically, this is CORRECT and current** — the doc does NOT propagate the stale ChaCha20/X25519 claim that the READMEs still carry. The one nuance: "moving toward FIPS" understates it — the algorithm swap already landed; what's outstanding is CMVP-validated *modules* (peat-btle.md:14, issue #75) and ADR-060 is formally still Proposed (peat.md:163). | Tighten: AES-256-GCM + ECDH-P256 are **already in use** (FIPS-approved algorithms); the remaining FIPS gap is module validation (CMVP / aws-lc-rs), not the algorithm choice. ADR-060 §5 is implemented though the ADR is formally Proposed. |
| 27 | Generic crypto types (`DeviceKeypair`, `EncryptionKeypair`, `FormationKey`) live in `peat_mesh::security` and are re-exported; peat-protocol adds policy on top | §2.7, lines 299-300 | **Verified** | peat.md:29 — peat-protocol's `security/*` modules are thin `pub use peat_mesh::security::…::*` shims (`encryption.rs:7`). **Shipped.** | None. |
| 28 | Whitepaper six-level authority model (Root → Cluster → Formation → Group → Team → Node) maps to membership-cert hierarchy levels | §2.7, lines 317-318 | **Unverifiable / Misleading** | The whitepaper authority ladder is not in the audited code. The actual authority enum is `AuthorityLevel` in `peat-schema/proto/node.proto:61-67` = `UNSPECIFIED, OBSERVER, ADVISOR, SUPERVISOR, COMMANDER` (peat.md:85) — a **five**-level ladder, not six, and with entirely different names. The doc presents a 1:1 "maps to" that the code does not support. | Either cite the whitepaper as the source and mark it as older framing not present in code, or replace with the real `AuthorityLevel` enum (OBSERVER/ADVISOR/SUPERVISOR/COMMANDER). Do not assert a clean mapping that doesn't exist. |
| 29 | "the **canonical current enum** is `peat_mesh::beacon::HierarchyLevel` = Platform / Cell / Cohort / Federation / Coalition (ADR-066)" | §2.7, lines 319-321 | **Wrong** | peat-mesh.md:101-115 — `HierarchyLevel` in `peat-mesh/src/beacon/types.rs:56-67` is `Node=0, Cell=1, Cohort=2, Federation=3, Coalition=4`. The leaf level is **`Node`, NOT `Platform`.** ADR-066's mapping renames Squad→Cell etc. but the peat-mesh leaf is `Node`; the `Platform→Node` rename (ADR-068, #968) is still churning. Calling "Platform/Cell/Cohort/Federation/Coalition" the *canonical current* enum is doubly wrong: (a) the enum says `Node` not `Platform`, and (b) ADR-066 is **Proposed**, not canonical (peat.md:165). Worse, the peat-protocol-side `authorization.rs:331-343` enum is `Node, Cell, Cohort, Federation, Coalition` too — so neither layer uses "Platform." | Replace with the actual enum: `peat_mesh::beacon::HierarchyLevel = Node / Cell / Cohort / Federation / Coalition`. Flag that ADR-066 is **Proposed** and the base-unit name is mid-rename (Platform vs Node, ADR-068/#968/#904); the proto/schema still ships legacy `squad_id`/`SquadSummary` (peat.md:77). Do not present any single vocabulary as settled-canonical. |
| 30 | "Trust as data — authority is replicated CRDT state… delegation and revocation propagate through the hierarchy like any other document" | §2.7, lines 311-315 | **Misleading / Speculative-unlabeled** | The "authority/membership certs replicate as CRDT documents that revocation propagates through" is a design narrative. Membership certificates exist (`security/membership.rs`, ADR-048) but ADR-048 and the RBAC/authorization model are not confirmed as live replicated-CRDT revocation in the ground truth; peat.md:180 notes **#941 Authorization model deferred pending Layer-1 device identity.** Presented as settled fact. | Label the replicated-trust/revocation-propagation story as **design intent (ADR-048, Proposed)**; note the authorization model is deferred (#941) and revocation propagation is not a verified shipped path. |
| 31 | RBAC: "User auth & RBAC — `user_auth.rs`, `authorization.rs` (roles, authority levels)" | §2.7, line 295 | **Verified (files) — but adjacent README role-names are a known trap** | `authorization.rs` exists and defines `Role { Leader, Member, Observer, Commander, Admin }` (verified directly, `src/security/authorization.rs:50-64`). The doc here does NOT enumerate the five role names, so it avoids the README's documented error (README claims "Observer, Member, Operator, Leader, Supervisor" — `Operator`/`Supervisor` are NOT in the enum, peat.md:82-85). | Optional improvement: since Module 2 is the security teaching module, list the *correct* five RBAC roles (Leader/Member/Observer/Commander/Admin) and explicitly warn that the README's "Operator/Supervisor" naming is wrong (conflated with the `AuthorityLevel` ladder). |
| 32 | CoT: `cot` module translates Peat messages → Cursor-on-Target XML with MIL-STD-2525 symbols + `<_peat_>` detail; consumed by ATAK/WebTAK/TAK Server; HTTP/socket plumbing in `peat-transport/src/tak/` | §2.8, lines 327-332 | **Verified** | peat.md:45 — TAK/CoT TCP bridge **Shipped** (`peat-transport`, `tokio-rustls`, `src/tak/`, ADR-020/028/029); `cot` module in public surface (peat.md:128). | None. **House-rule note:** the doc says "ATAK/WebTAK" — "WebTAK" is a TAK ecosystem product, generally acceptable as a protocol/client name, but per the prompt's no-consumer-vendor-name rule consider whether "ATAK" should be softened to "a CoT/TAK client." Flag only. |
| 33 | DEVELOPER_GUIDE §-references (§4.2-4.3, §3.4, §9.3) cited as sources for the capability/CRDT tables | §2.3, §2.5 (lines 153, 243, 261) | **Unverifiable** | The DEVELOPER_GUIDE was not in the ground-truth set and not read in this audit; cannot confirm these section numbers or that they say what the tables claim. The CRDT-per-type table (#20) is independently shown to be misleading regardless of the cited source. | Verify DEVELOPER_GUIDE section numbers exist and match; if the guide itself is stale, the citation launders an incorrect claim. |

## Summary of highest-impact issues (for the rewrite)

1. **Claim #29 (Wrong, high-risk):** the "canonical current" `HierarchyLevel` is stated as `Platform/Cell/Cohort/Federation/Coalition`. The actual peat-mesh enum leaf is **`Node`**, ADR-066 is **Proposed**, and the base-unit name is mid-rename (ADR-068/#904/#968). This is exactly the hierarchy-vocabulary trap the prompt warned about.
2. **Claim #20 (Misleading, high-risk):** the "which CRDT for which data" table implies peat-protocol selects discrete typed CRDTs (G-Set/OR-Set/PN-Counter/LWW-Map) per collection. Reality: peat-protocol stores **Automerge documents** (one CRDT family); the named primitives are peat-lite's embedded types, and even there OrSet/PnCounter are not real (firmware-only / reserved-byte). Must be relabeled illustrative.
3. **Claim #12 (Wrong):** `Weapon` is not a `CapabilityType` — the enum is Sensor/Compute/Communication/Mobility/Payload/Emergent (`capability.proto:11-19`). Remove or relabel the row.
4. **Claim #28 (Unverifiable/Misleading):** the six-level "Root→Cluster→Formation→Group→Team→Node" authority model has no code basis; the real `AuthorityLevel` is a 5-value ladder (OBSERVER/ADVISOR/SUPERVISOR/COMMANDER).
5. **Claims #22, #30 (label gaps):** QoS figures are configured *targets* not enforced SLAs (enforcement in-flight); "trust as data" revocation propagation is design intent (ADR-048 Proposed, authorization deferred #941). Per the prompt, neither carries a shipped/proposed label in the doc.
6. **Bright spots:** the deterministic-leader-election (#8), formation-handshake (#25), FIPS crypto (#26 — correctly AES-256-GCM/ECDH-P256, not the stale ChaCha20), and the negentropy-is-the-real-anti-entropy framing (#19) are all accurate and current — the doc avoids the worst README traps in these areas.
