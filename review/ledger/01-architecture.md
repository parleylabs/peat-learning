# Claim Ledger — Module 01 · Architecture Overview

**Target:** `learning/01-architecture-overview.md`
**Audited against:** ground-truth files in `learning/review/ground-truth/` (peat HEAD `35d0f11`, peat-mesh `00ab0c9`/rc.42, peat-btle `3d70f48`/0.4.0, peat-lite `7a8a8fb`/0.2.5, peat-gateway `8d16824`/0.1.0, peat-node `4e1b5c8`/0.4.3).
**Verdicts:** Verified · Outdated · Misleading · Oversimplified · Wrong · Unverifiable · Speculative-unlabeled.

| Claim (quote) | Location | Verdict | Evidence (path:line / ADR / issue) | Fix |
|---|---|---|---|---|
| "Sensors run on ESP32 microcontrollers with 256 KB of RAM." | §1.1, L12 | Oversimplified | peat-lite "256 KB" is a *design target / marketing figure*, not enforced or measured (`peat-lite.md` §9: README.md:7,13, lib.rs:21 — "no allocator cap, no static-RAM assertion"). | Keep as illustrative; add "design target, not an enforced budget." |
| "**Any transport works** — QUIC, BLE mesh, UDP, HTTP, simultaneously, with automatic failover." | §1.1, L21 | Misleading | Shipped transports are QUIC/Iroh, BLE (peat-btle), peat-lite UDP bridge, HTTP/REST, TAK/CoT (`peat.md` §1; `peat-mesh.md` §1). But "automatic failover" across all of them is not established — multi-transport PACE failover is README Roadmap **Planned/Future = Proposed** (`peat-mesh.md` §10: README.md:180-189). "UDP" as a peer transport overstates `UdpBypassChannel`, a primitive, not a `MeshTransport` (`peat-mesh.md` §1, §11). | Label transports shipped; mark "simultaneous + automatic failover (PACE)" as in-flight/proposed; don't list raw "UDP" as a peer transport. |
| "state is CRDT-based (via Automerge), so there is no central server and the mesh keeps operating through network partitions." | §1.1, L22-23 | Verified | Automerge over Iroh shipped (`peat-mesh.md` §2.1); partition detection + autonomous operation shipped (`peat-mesh.md` §8 topology). | None. |
| "**Scales** — hierarchical aggregation means the protocol that works for 5 nodes also works for 1,000+." | §1.1, L24-25 | Misleading | "1,000+" is a single-machine **simulation** ceiling (1023 hard limit, Linux bridge), not a field deployment; open epics #724-#727 still target 900/1.2K/10K (`peat.md` §9). Not asserted in peat-mesh/lite/btle/node/gateway code (`peat-mesh.md` §9, `peat-node.md`, `peat-gateway.md` §10). | State "validated in single-machine simulation to ~1,000 nodes (1023 cap); field/scale validation in-flight #724-#727." Label as simulation. |
| "the code is literally organized around them" (three phases: Discovery, Cell Formation, Coordination/Hierarchy) | §1.2, L29-30 | Verified | Discovery (`peat_mesh::discovery`, mDNS/static/K8s — `peat-mesh.md` §8), formation (HMAC handshake `peat.md` §7), hierarchy modules (`peat.md` §4) all shipped; Lens B diagram labels P1/P2/P3 (target doc L96). | None. |
| "form parent/child relationships (cell → cohort → federation → coalition in the current vocabulary)" | §1.2, L42-44 | Misleading | ADR-066 vocab is **Platform/Cell/Cohort/Federation/Coalition** and ADR-066 is **Proposed** (`peat.md` §4, §10). The chain here drops the base unit *and* states it as settled "current vocabulary." Reality is mid-rename: peat-mesh leaf is `Node` (`peat-mesh.md` §4: beacon/types.rs:56-67), `authorization.rs` uses Node/Cell/Cohort/Federation/Coalition, peat-btle still uses **Platform/Squad/Platoon/Company** (`peat-btle.md` §0/§4: lib.rs:495-507), proto still ships Squad/Platoon. ADR-068 proposes Platform→Node (#968/#904/#970). | Add the base unit; label "proposed (ADR-066), mid-rename"; note legacy military terms still in peat-btle/proto. |
| "A **cell** is the fundamental unit of organization." | §1.2, L42 | Oversimplified | True for formation hierarchy, but the *base* tier below cell is `Node`/`Platform` (`peat-mesh.md` §4; `peat.md` §4). Cell groups nodes (4-13 per `peat-mesh.md` §9: beacon/types.rs:59-65). | Clarify cell = grouping of nodes; node/platform is the leaf. |
| Lens A 5-layer crate diagram (Schema → Protocol → Transport → Binding → Application) | §1.3 Lens A, L52-83 | Verified | Sourced to `peat/docs/ARCHITECTURE.md` + whitepaper §4.3; matches member-crate roles (`peat.md` §0 table). Doc itself flags the lite Binding-vs-Transport editorial inconsistency. | None (already labeled as a packaging lens with date). |
| "peat-transport (HTTP/TAK)" placed in TRANSPORT layer | §1.3 Lens A, L64-75 | Verified | peat-transport = HTTP/REST (Axum) + TAK/CoT TCP bridge, shipped (`peat.md` §0,§1: README:184, src/tak/). | None. |
| Lens B 4-layer runtime stack, sourced to DEVELOPER_GUIDE.md §3.1 (2025-12-08) | §1.3 Lens B, L86-126 | Unverifiable | DEVELOPER_GUIDE.md not in ground-truth set; cannot confirm §3.1 content or the 2025-12-08 date. Layer names (Protocol/Storage abstraction/Network) are plausible vs `peat.md` §8 module list but the specific diagram/date is uncited here. | Verify against the actual DEVELOPER_GUIDE.md before publish; otherwise mark source unconfirmed. |
| Runtime data-flow mermaid: Local change → Delta generator → Priority assigner (QoS P1–P5) → Sync engine (Automerge) → Hierarchical router → Peer state | §1.3, L113-120 | Unverifiable | QoS 5-class ranking is real (Critical=1…Bulk=5, `peat-node.md`: peat-mesh/src/qos/mod.rs:70-101) and Automerge sync is real, but this exact pipeline ("Delta generator","Priority assigner" as discrete stages) is sourced only to the unverified DEVELOPER_GUIDE diagram. QoS *enforcement* (TTL/bandwidth, P1 latency) is in-flight (`peat.md` §9,§12). | Confirm against DEVELOPER_GUIDE.md; flag QoS enforcement as in-flight. |
| "developer guide expands the name as *Peat (Hierarchical Intelligence for Versatile Entities)* — which actually spells **HIVE**" | §1.3, L132-136 | Unverifiable | HIVE legacy naming is corroborated indirectly (ADR-049 "HIVE mesh extraction" `peat.md`; peat-lite ADR-001 `001-hive-lite-primitives.md` `peat-btle.md` §9 / `peat-lite.md`). But the exact "Hierarchical Intelligence for Versatile Entities" expansion is not in any ground-truth file. | Cite the developer-guide line directly or soften to "the project's former name HIVE." |
| Repo table: `peat-gateway` = "Enterprise control plane: multi-tenant enrollment, CDC, identity federation" | §1.4, L145 | Verified | Matches `peat-gateway.md` §1 (control plane, NOT a node/data-plane), §8 (enrollment), §2 (CDC), §2 (OIDC identity federation). | Optionally add "not a mesh node / not a data plane" — a key framing (`peat-gateway.md` §13.1). |
| Repo table: `peat-mesh` = "QUIC/Iroh P2P, Automerge CRDT sync, discovery, topology" + binary `peat-mesh-node` | §1.4, L143 | Verified | `peat-mesh.md` §1,§2,§8. Binary present (broker feature). | None. |
| Repo table: only "five repositories" listed | §1.4, L138-146 | Outdated | A sixth audited repo, **`peat-node`** (sidecar/node binary, gRPC API, v0.4.3), exists and is a real consumer surface (`peat-node.md`). Module presents the system as five repos. | Add peat-node as a sixth repo (sidecar exposing peat-protocol over gRPC). |
| "`peat/docs/adr/` — ~60 Architecture Decision Records." | §1.4, L158 | Unverifiable | Ground truth does not count total ADRs; #695 references "triage 22 Proposed ADRs" (`peat.md` §10,§11), implying many more exist but no total confirmed. | Soft-flag the "~60"; verify by `ls peat/docs/adr/`. |
| "`peat-discovery`" implied still present (not mentioned, but module omits its retirement) | §1.4 / §1.5 | Outdated (omission) | `peat-discovery` was **removed under peat#919**; discovery now lives in `peat_mesh::discovery` (`peat.md` §0). Module never lists peat-discovery, so not a false claim, but a reader cross-referencing old docs needs the note. | Optionally note discovery moved into peat-mesh. |
| `peat-schema` → (nothing) — the foundation | §1.5, L181 | Verified | `peat.md` §0 (Protobuf, prost); `peat-mesh.md`/manifests confirm schema has no Peat deps. | None. |
| `peat-protocol` → `peat-schema`, `peat-mesh`, `peat-btle` (optional, `bluetooth`); lib.rs `pub use peat_mesh; pub use peat_schema;` | §1.5, L182-184 | Verified | `peat.md` §0 (re-export shims), §8 (lib.rs:105-106 re-exports peat_mesh + peat_schema); §1 (bluetooth → peat-btle). | None. |
| `peat-mesh` → `peat-lite` (optional), `peat-btle` (optional, `bluetooth`) | §1.5, L189 | Verified | `peat-mesh.md` §1 (lite-bridge → peat-lite; bluetooth → peat-btle 0.4.0). | None. |
| `peat-btle` → `peat-lite` (optional, `peat-lite-frame`); does NOT depend on peat-mesh | §1.5/§1.6, L190, L247 | Verified | `peat-btle.md` §1 (peat-btle 0.4.0 has zero peat-mesh dependency; ADR-059 Amendment 4 broke the back-edge; `peat-lite-frame` → peat-lite 0.2.5). | None. |
| `peat-lite` → (nothing Peat), only `heapless`, no_std leaf | §1.5/§1.6, L191, L245-246 | Verified | `peat-lite.md` §1 (single runtime dep `heapless` 0.8; no_std). | None. |
| `peat-gateway` → `peat-mesh` (pinned exact `=`, features `automerge-backend` + `broker`) | §1.5, L192 | Verified | `peat-gateway.md` header + §2 (`=0.9.0-rc.1`, features automerge-backend + broker, Cargo.toml:23). | Add: pin is **stale** (`=rc.1` vs ecosystem rc.42 — gateway lags ~40 RCs, `peat-gateway.md` A2). |
| "ADR-049 'HIVE mesh extraction'" and "peat-mesh ADR-0002 'peat-mesh-extraction'" caused the arrow inversion | §1.5, L210-214 | Verified | peat-mesh repo-local `docs/adr/0002 peat-mesh extraction` (`peat-mesh.md` §10). ADR-049 referenced as HIVE extraction across repos. | None. |
| "The old `peat/docs/ARCHITECTURE.md` (2025-01-07) still draws `peat-mesh → peat-protocol` ... current README and Cargo.toml show `peat-protocol → peat-mesh`. Trust the manifests." | §1.5, L216-220 | Verified | Matches the documented code-ahead-of-doc pattern: peat-protocol is now a facade re-exporting peat-mesh (`peat.md` §0; lib.rs:105-106). | None. |
| "The fix (ADR-059 Amendment 4) **broke the back-edge**: `peat-btle` no longer depends on `peat-mesh`" | §1.5, L227-231 | Verified | `peat-btle.md` §1 (Translator trait impl removed in 0.4.0, back-edge break, ADR-059 Amendment 4 Slice 4.b; peat-btle has zero peat-mesh dep); `peat.md` §1 (peat#828 tracks Amendment 4). | None. Note ADR-059 itself is **Proposed** (`peat.md` §10). |
| "the `peat` workspace has to pin their versions carefully ... a long succession of release-candidate floors" | §1.5, L223-231 | Verified | peat HEAD `35d0f11` = "raise peat-mesh dependency floor to rc.42 (#990)" (`peat.md` header); long resolved-comment block in peat/Cargo.toml (`peat.md` §1). | None. |
| "**`peat-lite` and `peat-btle` are standalone leaf crates.**" | §1.6, L238-260 | Verified (with nuance) | Both are leaves: peat-lite zero Peat deps; peat-btle's only Peat dep is optional peat-lite (`peat.md` §1; `peat-btle.md` §1; `peat-lite.md` §1). Note peat-btle is "almost a leaf" — module correctly says so at L248. | None. Accurate. |
| "the team captured during the IWRP proposal work" (framing for why modularity matters) | §1.6, L262-264 | Unverifiable | No ground-truth reference to "IWRP proposal." Provenance unconfirmed. | Cite the source or drop the attribution. |
| "Each is its own crate on crates.io (and Maven Central), released on its own cadence." | §1.6, L271-272 | Unverifiable | Independent versioning is real (peat-mesh rc.42, peat-btle 0.4.0, peat-lite 0.2.5, distinct versions — ground-truth headers). crates.io presence is plausible (peat-mesh pulled via Cargo.lock) but **Maven Central** publication is not confirmed in any ground-truth file (peat-btle/peat-lite have Android AAR/UniFFI surfaces but publication channel unstated). | Confirm crates.io + Maven Central publication; otherwise soften. |
| "`peat-lite` compiles for bare-metal microcontrollers and `peat-btle` for mobile/embedded platforms" | §1.6, L271-273 | Verified | peat-lite no_std + ESP32 firmware (`peat-lite.md` §1); peat-btle multi-platform incl. ESP32/NimBLE, Android AAR, Apple FFI (`peat-btle.md` §1,§6). | None. |
| Checkpoint: "Which single crate does an application developer add to `Cargo.toml`, and what does it re-export?" (implied answer peat-protocol) | §1.4 L149 / Checkpoint L302 | Verified | peat-protocol is the SDK facade re-exporting peat_mesh + peat_schema (`peat.md` §0,§8). Caveat: `peat` umbrella crate is a **reserved-name placeholder with zero deps** (`peat.md` §0) — consumers depend on peat-protocol, not `peat`. | Optionally note the `peat` crate itself is a placeholder. |
| "Run `grep -rn \"pub use peat_mesh\" peat/peat-protocol/src/lib.rs`" (Try-it step) | Try it, L292 | Verified | lib.rs:105 `pub use peat_mesh;` confirmed (`peat.md` §8). | None (good hands-on exercise). |
| Try-it numbering: two steps labeled "2" | Try it, L290-293 | Wrong (editorial) | Steps mis-numbered (1, 2, 2, 3). Cosmetic but a skeptical reader notices. | Renumber 1-4. |
| Module scope omits FIPS/crypto, RBAC roles, MLS, leader election (deferred to later modules) | whole module | Verified (scoping OK) | These are Module 02/02b/03 topics per the prompt §4 mapping; Module 01 is layering/deps/modularity. No crypto claims made here to be wrong. The high-risk FIPS/ChaCha20, command_log, deterministic-election, SBD/LoRa-crate findings land in later modules — flagged here for cross-module consistency (`peat.md` §5,§6,§7; `peat-mesh.md` §6). | None for Module 01; ensure downstream modules carry the corrections. |
