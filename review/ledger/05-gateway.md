# Claim Ledger — Module 05 `learning/05-peat-gateway.md` (peat-gateway)

Audited against `learning/review/ground-truth/peat-gateway.md` (peat-gateway HEAD `8d16824`, crate `0.1.0`, peat-mesh pin `=0.9.0-rc.1`) and the peat-mesh ground-truth file for cross-layer identity/FIPS/hierarchy checks.

Verdicts: Verified · Outdated · Misleading · Oversimplified · Wrong · Unverifiable · Speculative-unlabeled.

| Claim (quote) | Location | Verdict | Evidence | Fix |
|---|---|---|---|---|
| "the gateway **observes and manages**; it does **not** sit in the data path. CRDT sync, blob transfer, and peer routing all stay in `peat-mesh`." | 5 (mental model, :8-10) | Verified | GT §1: gateway is NOT a mesh node, NOT a data plane; `src/api/formations.rs:28-30` doc comment confirms external nodes register broker state. | None. Core framing is correct. |
| "The gateway is optional — meshes work without it" | 5 (:9-10), repeated 5.1 ":Is not… or required" (:19-20) | Verified | GT §1 framing; gateway depends on peat-mesh as a library, not vice-versa. | None. |
| "identity federation (Keycloak / Okta / Azure AD / CAC-SAML)" | 5.1 (:15-16) | Misleading + house-rule violation | (a) House rule §8/§1 bans consumer/vendor names in generic protocol material — Keycloak/Okta/Azure AD are vendor names. (b) GT §2: what ships is **OIDC token introspection (RFC 7662)**, feature `oidc`, `src/api/enroll.rs:305-392`. SAML/CAC is NOT confirmed shipped anywhere in GT — only OIDC is. | Replace vendor list with "any OIDC-compliant IdP (RFC 7662 token introspection)". Drop CAC-SAML or label it Unverified/Proposed — GT has no evidence SAML/CAC is implemented. |
| "Change Data Capture (CDC) streaming" as a provided capability | 5.1 (:15) | Verified | GT §2/§3: CDC watcher + engine ship; observes `peat_mesh` `DocumentStore` change events, emits flat `CdcEvent`. | None, but should be labeled Shipped. |
| "Zarf/UDS packaging for air-gapped deployment" | 5.1 (:16-17), 5.5 (:170-175) | Verified | GT §1: repo ships `zarf.yaml` and a UDS bundle (`bundle/`) plus Helm `chart/`. | None. Could note this is packaging only, not a security guarantee. |
| `main.rs` "three subcommands: Serve / MigrateKeys / LoadTest" | 5.2 (:24-28) | Verified | GT §7: `migrate-keys` CLI re-seals plaintext genesis (`src/cli.rs`, `src/main.rs:22`); GT §10: loadtest behind `loadtest` feature (`src/loadtest.rs`). | None. |
| serve() wires "tenant manager, CDC engine, router" | 5.2 (:30-41) | Verified | Matches GT §8 library surface (`api`, `cdc`, `tenant`) and the router assembly in `src/api/mod.rs:71-93`. | None. |
| `src/lib.rs` declares `api, cdc, cli, config, crypto, ingress (NATS), storage, tenant` | 5.3 (:45-46) | Verified | GT §8: `src/lib.rs` modules are `api, cdc, cli, config, crypto, ingress (nats), storage, tenant`. | None. Could add the `#![allow(dead_code)]` "Scaffolding — stubs will be wired incrementally" candor signal (GT §8, `src/lib.rs:1`). |
| `MeshGenesis` is "`peat-mesh`'s root-of-trust authority object" created per formation | 5.3 (:55-65) | Verified | GT §4: `MeshGenesis::create(&app_id, mesh_policy)` at `src/tenant/manager.rs:234-237`; genesis stored envelope-encrypted. | None. |
| `to_mesh_tier`: Authority→Enterprise, Infrastructure→Regional, Endpoint→Tactical | 5.3 (:67-77) | Verified (incomplete) | GT §5: mapping table matches exactly (`src/tenant/models.rs:179-188`). BUT GT §5/§12 flags the gateway maps only **3 of the 4** mesh tiers — `Edge` is unreachable from the gateway. | Add a note: the mesh wire enum is `Enterprise/Regional/Tactical/Edge`; the gateway never issues `Edge`. |
| Implicit: gateway `MeshTier` (Authority/Infrastructure/Endpoint) is the hierarchy vocabulary | 5.3 throughout; "enterprise roles onto mesh trust tiers" (:67) | Verified (and correctly NOT claimed as ADR-066) | GT §5/§13: gateway tier = RBAC role concept; mesh wire tier = distribution-tier concept; neither is ADR-066 Platform/Cell/Cohort/Federation/Coalition. The module correctly calls these "enterprise roles"/"trust tiers" and does NOT conflate them with the formation hierarchy. | None — but to be safe, add one sentence stating these tiers are NOT the ADR-066 formation hierarchy, since adjacent modules use that vocabulary. |
| RBAC framing only via tier names; no perms detail | 5.3 (:67-81) | Oversimplified | GT §5: actual RBAC is a u32 bitmask `RELAY=0x01, EMERGENCY=0x02, ENROLL=0x04, ADMIN=0x08` (`src/tenant/models.rs:191-216`), narrowed to mesh u8. Module omits this entirely. | Optional: mention permissions are a bitmask, not the "five named roles" some modules reference. |
| Envelope encryption: per-record DEK wrapped by KEK; `KeyProvider` trait `wrap_dek`/`unwrap_dek` | 5.3 (:83-95) | Verified | GT §6/§7: trait contract `wrap_dek`/`unwrap_dek` (`src/crypto/mod.rs:37-41`); PENV v1 versioned envelope format. Best-tested subsystem. | None. |
| Four backends: Plaintext, LocalKeyProvider (AES-256-GCM), AwsKmsProvider, VaultTransitProvider | 5.3 (:97-104) | Verified | GT §7: four backends — Local AES-256-GCM, AWS KMS, Vault Transit, Plaintext (errors on wrap). | None. |
| Implicit FIPS posture: local backend "AES-256-GCM KEK" | 5.3 (:102) | Verified (caveat omitted) | GT §6: repo is FIPS-clean, zero ChaCha20; AES-256-GCM only (`aes-gcm 0.10`). BUT GT §6/§13 caveat: local-KEK uses **non-CMVP software AES** — for a real FIPS 140 boundary use KMS/Vault HSM backends. | Add the caveat: local-KEK path is not a FIPS-validated module; KMS/Vault is the FIPS-boundary path. Note this is the one repo with NO ChaCha20 issue (do not let readers attribute the peat-btle ADR-052 ChaCha problem here). |
| CDC "watching the mesh's document store" via `peat_mesh::sync::traits::DocumentStore` / `ChangeEvent` | 5.3 (:109-119) | Verified | GT §3: watcher subscribes via `DocumentStore::observe()`, consumes `ChangeEvent` (`Initial`/`Updated`/`Removed`) at `src/cdc/watcher.rs:6-7,76-206`. | None. |
| "Kafka and Redis Streams are supported via optional deps (`rdkafka`, feature-gated)." | 5.3 (:125) | Wrong (over-advertised) | GT §2/§12/§13: **Kafka is a TODO stub** — `src/cdc/engine.rs:78-80` is `// TODO: Kafka delivery`; the dispatcher branch is empty, so Kafka does NOT deliver. **Redis does not exist at all** — no `CdcSinkType::Redis` variant, no feature, no code (`src/tenant/models.rs:79-84` is `Nats`/`Kafka`/`Webhook` only). README over-advertises both. | State plainly: only NATS JetStream and Webhook are Shipped sinks. Kafka is In-flight/stubbed (TODO, does not deliver). Redis is Proposed/absent. Do not say "supported". |
| "Delivery is at-least-once." | 5.3 (:127) | Unverifiable | GT does not state a delivery-semantics guarantee for the CDC engine; ingress has DLQ/max_deliver (GT §9) but the CDC *egress* at-least-once claim is not sourced in GT. | Either cite the code path that guarantees at-least-once (retry/cursor) or flag as Unverified. The `change_hash` SipHash cursor (GT §3, `watcher.rs:238-258`) supports restart-dedup, which is adjacent but not the same guarantee. |
| Mermaid CDC diagram shows `E → K[NATS… Kafka]` and `E → R[Redis Streams]` as working outputs | 5.3 (:129-137) | Misleading | Same as above (GT §2): Kafka does not deliver; Redis does not exist. Diagram presents 4 equal sinks. Also no legend (Prompt §8 requires a legend on every diagram). | Mark Kafka (stubbed) and Redis (proposed/absent) distinctly (dashed/greyed) and add a legend. Only NATS + Webhook are solid. |
| Enrollment "delegate to an enterprise IdP (OIDC via `openidconnect`, or SAML/CAC)" | 5.3 (:139-144) | Misleading | GT §2: OIDC token introspection (RFC 7662) is Shipped (`src/api/enroll.rs:305-392`). SAML/CAC is NOT evidenced in GT. The `openidconnect` crate name is not confirmed in GT (GT cites the RFC 7662 flow, not a specific crate). | Drop SAML/CAC unless sourced, or label Proposed/Unverified. Verify the `openidconnect` crate name against `Cargo.toml` before asserting it. |
| "`PolicyRule` / `EnrollmentPolicy` engine maps an authenticated identity's role onto a `MeshTier`, producing an `EnrollmentDecision`" | 5.3 (:142-144) | Oversimplified | GT §8: policies are `Open` (→ Endpoint/0-perms), `Controlled` (token OR OIDC + policy rules), `Strict` (**always 403 — admin approval NOT implemented**, `src/api/enroll.rs:89-93`). The mapping exists but `Strict` is a non-functional placeholder. | Note that `Strict` policy is unimplemented (403s). List the three policy modes. |
| "admin API is gated by `PEAT_ADMIN_TOKEN` (and warns loudly if unset)." | 5.3 (:144) | Verified (understated risk) | GT §8: bearer-token protected when set; **open in dev mode when unset** (README.md:143). | Sharpen: when the token is unset the admin API is fully open — call this out as a deployment hazard, not just a "warning". |
| Storage: `StorageBackend` trait, `postgres.rs` (sqlx, feature-gated) + `redb_backend.rs` (default) | 5.3 (:146-151) | Verified | GT §2: Postgres feature `postgres` (`src/storage/postgres.rs`) alt to default redb. | None. Could note Postgres integration tests not in CI (peat-gateway#53, GT §12). |
| Admin API route map: "`/healthz, /metrics` → health + Prometheus" | 5.3 (:152-161) | Wrong (route name) | GT §8: health route is **`GET /health`** (and `GET /metrics`), `src/api/health.rs:22-26`. Doc writes `/healthz`. | Change `/healthz` to `/health`. |
| Route map `/orgs → orgs, tokens, sinks, identity, formations (admin-token gated)`; `/orgs/.../enroll` public | 5.3 (:156-161) | Verified | GT §8: orgs/tokens/sinks/identity/formations under admin auth; `POST /orgs/{org}/formations/{app}/enroll` is public, no admin auth (`src/api/enroll.rs:466-470`). | None. Could note enroll is the public node-onboarding entrypoint. |
| `/_/ → SvelteKit admin UI served as static files` | 5.3 (:160), 5.4 (:163-168) | Verified | GT §8: static SvelteKit at `/_/` when `PEAT_UI_DIR` set (`src/api/mod.rs:48-56`). | None. Could add the `PEAT_UI_DIR` gating condition. |
| 5.4 UI feature list (orgs, formations, CDC sink config, enrollment tokens, IdP config, peer health, document browser, cert lifecycle) | 5.4 (:163-168) | Unverifiable | GT does not enumerate the SvelteKit UI's screens; it only confirms the UI exists and is served at `/_/`. The specific screen list is not sourced. | Flag as descriptive/unverified, or trim to what the API surface (GT §8) actually backs. |
| Helm chart + Zarf/UDS bundle for "air-gapped, SSO-integrated, network-policied deployment (Defense Unicorns' UDS platform)" | 5.5 (:170-175) | Verified (vendor name borderline) | GT §1: `chart/` + `zarf.yaml` + `bundle/` exist. "Defense Unicorns' UDS" names a platform vendor — UDS/Zarf are the platform proper nouns the repo targets, generally acceptable as packaging targets, but "SSO-integrated/network-policied" is an attribute of UDS, not proven by gateway code. | Keep UDS/Zarf (they are the actual packaging targets). Soften "SSO-integrated/network-policied" to "the UDS platform provides…" rather than implying the gateway code enforces it. |
| `Cargo.toml` pins `peat-mesh` to an exact `=`-version with features `["automerge-backend", "broker"]`; uses it purely as a library | 5.6 (:177-182) | Verified (staleness unflagged) | GT preamble + §13: pin is `=0.9.0-rc.1` features `automerge-backend, broker` (`Cargo.toml:23`). Library-only use is correct. BUT GT flags the pin is **~40 RCs stale** (ecosystem at rc.35/rc.42); SKILL.md:74 treats it as intentional. | Add: the pin is intentionally frozen and lags the live mesh by ~40 release candidates; bumps require full integration validation. Honest "shipped vs current" signal. |
| Checkpoint Q "What maps an enterprise role to a mesh trust tier?" (implies answer `to_mesh_tier`) | Checkpoint (:201) | Verified | Consistent with 5.3 mapping (GT §5). | None. |
| Whole-module: no shipped / in-flight / proposed / speculative labels anywhere | entire module | Speculative-unlabeled (process gap) | Prompt §1/§8/§11: every capability must carry a status label. Module 5 mixes Shipped (CDC, envelope crypto, OIDC, NATS), In-flight (Kafka), and Proposed/absent (Redis, SAML/CAC, Strict policy) with no labels. | Add explicit Shipped/In-flight/Proposed labels to the capability lists, the sink table, and the IdP claims. |
| Omission: no mention that enrollment performs NO proof-of-possession (trusts client-supplied pubkey + node_id) | 5.3 enrollment (:139-144) | Misleading (by omission) | GT §4/§12: enroll validates the 32-byte Ed25519 pubkey for **length only** (`src/api/enroll.rs:273-285`); node_id is an opaque client string, not derived/verified (`enroll.rs:54-56,229-235`). No signature challenge. | Add a security note: enrollment does not yet prove key possession — a known gap. |
| Omission: NATS control-plane ingress exists but AuthZ is a permissive stub + broker auth/TLS absent | not mentioned (module omits `ingress` security posture) | Misleading (by omission) | GT §9: ingress AuthZ is a PERMISSIVE STUB (peat-gateway#99), broker ACLs (#97), NATS auth/TLS (#124,#125) all OPEN. Module lists `ingress` module but says nothing about its non-production-secure state. | Add a paragraph: the NATS ingress is feature-rich but not yet production-secure (AuthZ stub, no broker auth/TLS). |
| Omission: no quantitative perf figures, and module makes no "1,000+ node" claim | n/a | Verified (clean by omission) | GT §10: the only numbers in-repo are self-reported debug-build microbenchmarks (~137 enroll req/s); NO "1,000+ node" claim exists here. Module 5 correctly cites no such figures. | None — if any perf numbers are later added, label them "vendor's own loadtest harness, debug build, not third-party validated." |
| Mental-model assumption: "a gateway… meshes work without it" relied on for the optional-control-plane framing | 5 mental model (:8-10) | Verified (assumption holds) | GT §1 confirms gateway is non-essential to the data plane. | None. |
