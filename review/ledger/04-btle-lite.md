# Claim Ledger — Module 04 `peat-btle` & `peat-lite`

**Target:** `learning/04-peat-btle-and-lite.md`
**Ground truth:** `learning/review/ground-truth/{peat-btle.md, peat-lite.md, peat-mesh.md}` + direct source reads at the audited HEADs (peat-btle `3d70f48` / 0.4.0, peat-lite `7a8a8fb` / 0.2.5, peat-mesh `00ab0c9` / 0.9.0-rc.42).
**Verdicts:** Verified · Outdated · Misleading · Oversimplified · Wrong · Unverifiable · Speculative-unlabeled

| Claim (quote) | Location | Verdict | Evidence | Fix |
|---|---|---|---|---|
| "microcontrollers with 256 KB of RAM" / "tiny enough to run … fits in 256 KB" | L4, L104-105 | Unverifiable | `peat-lite.md §9`: "256KB of RAM" is a design target/marketing figure (README.md:7,13; lib.rs:21); no allocator cap, no static-RAM assertion, not enforced or measured. | Label as a design target, not a measured/enforced budget. |
| "traditional BLE-mesh SDKs burn 20%+ radio duty cycle and give a watch 3–4 hours; `peat-btle` targets <5% and 18–24 hours" | L19-22 | Unverifiable | `peat-btle.md §8`: "<5% / 20%+" comparison and "18–24 h / 3–4 h" are uncited marketing (README.md:28-31). No in-repo measurement harness output. | Mark as uncited vendor-comparison marketing; either cite a source or drop the competitor numbers. Computed duty-cycle from `power/profile.rs:39-55` is the only defensible figure. |
| "a hierarchical 'leaf nodes don't scan' topology" | L21-22 | Unverifiable | Not located as a named mechanism in source during audit; power savings come from `PowerProfile` scan/adv timing (`power/profile.rs`), not a documented leaf-no-scan rule. | Soften to the verifiable mechanism (power-profile duty cycling) unless a leaf-no-scan code path is cited. |
| "`BluetoothLETransport` … implements the ADR-032 transport contract" | L27 | Verified | `peat-btle.md §1`: `BluetoothLETransport` implements `MeshTransport` (ADR-032) at `src/transport.rs:159-324`. | None. |
| "`BleConfig` — configuration (`peat_lite()`, `with_power_profile()`, `with_phy()`)" | L29 | Wrong | `peat_lite()` exists (`config.rs:428`); but `power_profile` is a public field applied via `apply_power_profile()` (`config.rs:398,439`) — there are NO `with_power_profile()` or `with_phy()` builder methods on `BleConfig`. | Drop `with_power_profile()`/`with_phy()` or replace with the real API (`power_profile` field + `apply_power_profile()`; PHY via `BlePhy`/`coded-phy`). |
| "`PeatMesh` — the mesh-management facade (ADR-039)" | L30 | Verified | `peat-btle.md §6`: `PeatMesh`/`PeatMeshConfig` high-level mesh object (`src/peat_mesh.rs:162-256`). | None (ADR-039 is referenced in code comments; repo-local ADRs differ — fine for the umbrella citation). |
| "`MeshManager` — topology, parent selection, failover" | L31 | Verified | `peat-btle/src/lib.rs:248` `pub use mesh::MeshManager;`. | None. |
| "`NodeId` — a 32-bit node id, often derived from the BLE MAC (`NodeId::from_mac_address`)" | L32 | Verified | `peat-btle.md §3`; `NodeId` is `u32` (`src/lib.rs:367-371`); `from_mac_address` (`src/lib.rs:408,446`). | None. **But** see the cross-cutting NodeId note below — this is peat-btle's u32, NOT the SHA-256/Ed25519 model; module never claims SHA-256 here, which is correct. |
| "the *same* mesh logic runs on six platforms" + platform table (Linux Stable, macOS stable/iOS beta, Android Stable incl. WearOS, Windows Planned, ESP32 Stable, Mock) | L37, L52-59 | Verified | `peat-btle.md §1` table matches: Linux/macOS/Android/ESP32 Shipped; iOS partial/beta; Windows in flight ("Planned — adapter exists, not yet tested", README.md:44). | None. Optionally note Windows GATT-server write path is not real (`windows/gatt_server.rs:227`) and iOS callbacks incomplete (`apple/central.rs:209`). |
| "BLE has tiny MTUs (often 23 bytes → ~15 bytes of payload per write)" | L67 | Verified | Standard BLE ATT default MTU 23B with 3B ATT header → 20B; "~15" is conservative but in the right range. Hardware assumption, not a PEAT constant. | Acceptable; could note this is the BLE spec default, not a PEAT figure. |
| "`ChunkHeader` — 8 bytes: `[message_id:4][chunk_index:2][total_chunks:2]`" | L70 | Verified | `src/sync/protocol.rs:38` `CHUNK_HEADER_SIZE = 8`; fields `message_id:u32`, `chunk_index:u16`, `total_chunks:u16` (`:42-68`). | None. |
| "`ChunkReassembler::process(chunk, now)` … (30 s partial-timeout)" | L71-72 | Verified | `src/sync/protocol.rs:152` `process(...)`; `partial_timeout_ms: 30_000` (`:145`). | None. |
| "`SyncMessageType` — `SyncVector`, `Document`, `Ack`, `EndSync`, `Error`" attributed to `peat-btle/src/sync/protocol.rs` | L68, L73 | Misleading | The variants are correct (`SyncVector, Document, Ack, EndSync, Error`) but the enum lives in `src/gatt/protocol.rs:68-77`, NOT `src/sync/protocol.rs` (which defines `SyncState{Idle,Sending,Receiving,WaitingAck}`, `:256`). The file citation header at L68 covers the wrong file for this type. | Correct the path: `SyncMessageType` is in `src/gatt/protocol.rs`. `ChunkHeader`/`ChunkReassembler`/`DeltaEncoder` are in `src/sync/`. |
| "`DeltaEncoder` / `PeerSyncState` (`sync/delta.rs`) … `needs_send(key, timestamp)` … on reconnect only changed keys go over the air" | L74-76 | Verified (with caveat) | `src/sync/delta.rs:32` `PeerSyncState`, `:69` `needs_send`, `:84` `DeltaEncoder`. **Caveat:** reconnect re-delivery of pending CRDT state is tracked OPEN (`peat-btle.md §7`, issue #73). The encoder tracks per-peer sent state; full reconnect re-delivery is In flight. | Add: re-delivery of pending state on reconnect is In flight (#73). |
| Mermaid sequence diagram (Central→Peripheral SyncVector / Document chunks / Ack / EndSync) | L79-89 | Verified | Matches `SyncMessageType` flow in `gatt/protocol.rs` (Vector→Document→Ack→End). | None. Diagram has no legend — add one per §8 HTML/diagram rule when rewriting. |
| "compact 16-byte `PeatBeacon` … fits in a legacy 31-byte BLE advertisement … version, capability flags, node id, hierarchy level, a 24-bit geohash (~600 m), battery %, and a sequence number" | L91-93 | Misleading | `BEACON_SIZE = 16` (`discovery/beacon.rs:60`) and geohash "24-bit, ~600m precision" (`:78`) are correct. **But** the beacon's own doc says total advertised payload is "3 + 18 + (3 + 16) = 40 bytes (**requires extended advertising**)" (`beacon.rs:42,47`) — it does NOT fit a legacy 31-byte advertisement as a complete advertised frame. The 16B is the beacon body, not the full AD. | Clarify: the 16-byte beacon *body* is carried in Service Data; the full advertisement needs extended advertising (~40B), not the legacy 31B limit. |
| "Phase 1 is a mesh-wide AES-256-GCM key derived via HKDF … Phase 2 adds per-peer end-to-end encryption via ECDH-P256, with Ed25519 device identities. All FIPS-approved primitives (ADR-060)." | L95-97 | Verified | `peat-btle.md §5`: AES-256-GCM (`mesh_key.rs:23-25`), ECDH-P256 (`peer_key.rs:33`), HKDF-SHA256 (`mesh_key.rs:124-128`), Ed25519 (`identity.rs`). Crate migrated off ChaCha20/X25519 (2026-05-18). | None — this is the corrected, FIPS-clean description. **Note:** the *README* still advertises ChaCha20/X25519 (stale, `README.md:196,218,242,282`); the module correctly uses the code reality. Optionally add the residual gap: no CMVP-validated module yet (issue #75, In flight); BLAKE3 used for NodeId derivation is non-FIPS but non-security. |
| "kilobytes of memory, no allocator. `peat-lite` provides bounded, `no_std` CRDT primitives … Its *only* dependency is `heapless`" | L103-105 | Verified | `peat-lite.md §1`: single runtime dep `heapless` 0.8 (Cargo.toml:38); `no_std`-capable. | None. |
| Primitives table: `NodeId` 4B u32 `#[repr(transparent)]` | L111 | Verified | `peat-lite.md §4`: `pub struct NodeId(u32)` `#[repr(transparent)]` (`node_id.rs:15`). | None. |
| `CannedMessage` 1 byte | L112 | Verified | `peat-lite.md §9`: `#[repr(u8)]`. | None. |
| `CannedMessageEvent` ~24 bytes | L113 | Oversimplified | Unsigned canned-message *wire* size is exactly 22 bytes (`wire.rs:21-22` `CANNED_MESSAGE_UNSIGNED_SIZE = 22`); the in-memory struct may differ. "~24" conflates and rounds. | Use the precise wire figure: 22B unsigned / 86B signed (`wire.rs:22,27`), or label "~24B in memory" explicitly. |
| `LwwRegister<T>` `sizeof(T)+12` | L114 | Verified | `peat-lite.md §3a`: `LwwRegister<T>` shipped (`lww.rs:33`); 12 = 8B timestamp + 4B node_id, consistent. | None. |
| `GCounter<const N=32>` ~260 bytes | L115 | Verified | `peat-lite.md §9`: GCounter default 32 nodes ≈ 260B (counter.rs:17-18). | None. |
| **`PnCounter<const N=32>` (`counter.rs`) ~520 bytes "Increment/decrement counter (two GCounters)"** listed as a peat-lite core primitive | L116 | **Wrong** | `peat-lite.md §3a, §11`: `PnCounter` is **NOT implemented in the core library** — `grep -c PnCounter peat-lite/src/counter.rs` = **0**. It exists only as (a) a wire-type byte `CrdtType::PnCounter=0x03` (`crdt_type.rs:8`) and (b) a **firmware-only** impl (`firmware/src/crdt/pn_counter.rs:14`), which is a separate crate a library consumer does NOT get. The "two GCounters" description matches the firmware impl, not the published crate. | Remove `PnCounter` from the core-primitives table, or relabel it: wire-type code only in core; struct exists only in the workspace-excluded `firmware/` crate. A library consumer gets `LwwRegister` + `GCounter` (+ canned-message hybrid CRDTs) only. |
| Primitives table omits `CannedMessageStore` and `CannedMessageAckEvent` | L109-116 | Oversimplified | `peat-lite.md §3a`: `CannedMessageStore` (LWW, `canned.rs:750`) and `CannedMessageAckEvent` (OR-set + LWW hybrid, `canned.rs:432`) are shipped core CRDTs not shown. | Add them; they are the actually-shipped CRDT surface beyond LWW/GCounter. |
| Canned-message code table (0x00–0x0F Acks; 0x10–0x1F Status CHECK IN/MOVING/HOLDING/ON STATION/RTB/COMPLETE; 0x20–0x2F Alerts; 0x30–0x3F Requests; 0xF0–0xFF Reserved) | L121-127 | Verified | `canned.rs:26,44-46` (Status 0x10-0x1F, `CheckIn=0x10`); `name()` returns "WILCO" (`:143`), "RTB" (`:150`). Ranges and sample codes consistent. | None (sample labels are illustrative; exact enum spelling differs e.g. `AckWilco`→"WILCO" but ranges are correct). |
| LWW `should_accept` code snippet (timestamp wins; tie → higher `node_id.as_u32()`) | L131-136 | Verified | `peat-lite.md §3a`: `lww.rs:96-99` LWW, ties broken by higher `node_id.as_u32()`. | None. |
| GCounter `merge` = per-node max snippet | L138-144 | Verified | `peat-lite.md §3a`: merge = per-node max (`counter.rs:92-104`). | None. |
| 16-byte header layout: MAGIC "PEAT" / version 0x01 / type / flags u16 LE (FLAG_HAS_TTL=0x0001) / node_id u32 LE / seq_num u32 LE | L154-161 | Verified | `peat-lite.md §6`: MAGIC "PEAT" (constants.rs:4), version 1, flags 2B LE, node_id 4B LE, seq 4B LE; FLAG_HAS_TTL=0x0001 (ttl.rs). | None. |
| "`MessageType` … `Announce 0x01` … `Leave 0x06`, `Document 0x07`, and OTA types `0x10–0x16`" | L163-164 | Verified | `peat-lite.md §6`: message types incl. `Document=0x07` and OtaOffer=0x10…OtaAbort=0x16 (`message_type.rs:13-49`). Module correctly includes `Document 0x07` — which the README omits. | None (module is more accurate than the README here). |
| "`CrdtType` … `LwwRegister 0x01`, `GCounter 0x02`, `PnCounter 0x03`, `OrSet 0x04`" | L165-166 | Verified (as wire bytes) | `peat-lite.md §3a`: `crdt_type.rs:6-11` exactly these four wire bytes. | None — **but** ensure the module does not imply `PnCounter`/`OrSet` are implemented structs (they are reserved/firmware-only/absent). See the L116 fix. |
| "Default UDP gossip port is 5555; max packet 512 bytes" | L166 | Verified | `peat-lite.md §9`: `DEFAULT_PORT=5555`, `MAX_PACKET_SIZE=512` (constants.rs:13,22). | None. |
| "The `Document 0x07` type carries a *universal* `peat_mesh::Document` envelope (collection + id + length-prefixed body) — opaque to peat-lite, interpreted by peat-mesh" | L168-170 | Verified (with nuance) | `peat-lite/src/protocol/document.rs:1-47`: the envelope (collection + doc_id + timestamp + opaque body) is defined IN peat-lite and is transport-agnostic; the *body* is `peat_mesh::Document.fields` postcard-encoded (`document.rs:47`), owned by the publisher/consumer. The envelope codec ships in peat-lite (ADR-059 Amendment 4 "universal half"), not peat-mesh. | Tighten: the **envelope codec is peat-lite's**; only the opaque **body schema** is `peat_mesh::Document`. Saying the envelope is "interpreted by peat-mesh" understates that peat-lite owns and tests the envelope codec. |
| "`firmware/` — a separate `no_std` crate … excluded from the main workspace … Contains the CRDT impls, protocol, a gossip state machine, and OTA. Targets like M5Stack Core2 and ESP32-S3" | L175-177 | Verified | `peat-lite.md §1, §3b`: firmware workspace-excluded, has its own CRDT impls incl. PnCounter (`firmware/src/crdt/`), OTA (`firmware/src/ota.rs`). | None. This is also the place to anchor the L116 fix (PnCounter lives here, not core). |
| "`fuzz/` — fuzz targets for the decoders (`fuzz_decode_header`, `fuzz_canned_message_ack_decode`)" | L178-179 | Verified | `peat-lite.md §8`: 5 cargo-fuzz targets. | None. |
| "`peat-lite` and `peat-btle` are standalone leaf crates: the rest of PEAT depends on them, but they depend on nothing in PEAT (beyond `peat-btle`'s one optional link to `peat-lite`)" | L186-194 | Verified | `peat-btle.md §1`: peat-btle has **zero peat-mesh dependency** as of 0.4.0; `peat-lite-frame` feature pulls `peat-lite 0.2.5` (`peat-btle/Cargo.toml:47,174`). peat-lite has no peat deps (`peat-lite.md §11`). | None. |
| "`peat-btle` → `peat-lite`: optional, behind the `peat-lite-frame` feature … carries the universal `Document` envelope (`MessageType::Document = 0x07`)" | L196-198 | Verified | `peat-btle/Cargo.toml:47` `peat-lite-frame = ["std", "dep:peat-lite"]`; `:174` `peat-lite = "0.2.5" … optional`. | None. |
| "`peat-mesh` → both: optional. `peat-mesh`'s `bluetooth` feature integrates `peat-btle`; its `lite-bridge` feature bridges `peat-lite` UDP devices" | L199-200 | Verified | `peat-mesh.md §1`: BLE shipped via feature `bluetooth` (delegates to peat-btle); Peat-Lite UDP bridge shipped via feature `lite-bridge` (`src/transport/lite.rs:841`). | None. |
| "ADR-059 Amendment 4 deleted that back-edge: `peat-btle` now exposes codec primitives (behind `translator-codec`), and `peat-mesh` implements the `Translator` on top of them … broke cargo's resolver" | L201-206 | Verified | `peat-btle.md §1`: `Translator` trait impl removed from peat-btle in 0.4.0 (ADR-059 Amendment 4 Slice 4.b), now lives in peat-mesh; `translator-codec` feature provides codec scaffolding; peat-btle has zero peat-mesh dep. `peat-mesh.md §8`: `Translator`+`BleTranslator` shipped. | None — accurate and well-cited. |
| Dependency diagram (peat-mesh→peat-btle→peat-lite; lite-bridge edge; "peat-lite has NO peat deps") | L208-213 | Verified | Consistent with the dependency facts above. | Add a legend (feature-gated edges vs hard edges) when rendered in the hub HTML. |
| "Try it" / "Checkpoint" references (`lww.rs`, `counter.rs`, `protocol/header.rs`, `sync/protocol.rs` `ChunkReassembler::process`, examples `linux_scanner`/`canned_message_sync`/`peer_e2ee`) | L219-232 | Verified | Files exist as cited; `ChunkReassembler::process` (`sync/protocol.rs:152`). Examples corroborated by `peat-btle.md §6` (integration tests/examples incl. `peer_e2ee`). | None. |
| Cross-cutting — hierarchy vocabulary: module never invokes ADR-066 names for these crates | whole module | Verified (absence is correct) | `peat-btle.md §4`: peat-btle's `HierarchyLevel` is **legacy** Platform/Squad/Platoon/Company (`lib.rs:495-507`), NOT ADR-066; `peat-lite.md §4`: peat-lite has **no hierarchy enum**. The module wisely does not claim ADR-066 vocabulary for either crate. | If hierarchy is added later, flag that peat-btle uses legacy military vocabulary and peat-lite has no tier enum — do NOT present ADR-066 names as theirs. The beacon "hierarchy level" field (L92) maps to peat-btle's legacy enum. |
| Cross-cutting — no `command_log`, no anti-entropy/digest/IBLT/version-vector claimed for these crates | whole module | Verified (absence is correct) | `peat-btle.md §2` and `peat-lite.md §8`: no `command_log` anywhere; peat-lite has no sync engine/anti-entropy/leader election; peat-btle uses delta-sync + gossip, no leader election (`peat-btle.md §7`). Module makes no such claims. | None — but if §05/§06 introduce these, keep them out of the edge-crate description. |
| Cross-cutting — no shipped/in-flight/proposed labels in the module body | whole module | Misleading (omission) | Per §1/§8 every capability must carry a status label. The module presents iOS "beta", Windows "Planned" in the platform table (good) but states FIPS crypto, reconnect re-delivery (#73), CMVP module (#75) as settled without flagging In-flight items. | Add explicit Shipped/In-flight labels: BLE transport, mesh/peer crypto, delta-sync = Shipped; iOS full callbacks, Windows, reconnect re-delivery (#73), CMVP-validated module (#75) = In flight; peat-lite `PnCounter`/`OrSet` core impl, per-document encryption, SBD/LoRa = Proposed/absent. |
