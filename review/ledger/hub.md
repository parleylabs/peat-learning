# Claim Ledger — `learning/peat-learning-hub.html`

Audited against ground-truth files in `learning/review/ground-truth/` (peat HEAD `35d0f11`,
peat-mesh `00ab0c9`/rc.42, peat-btle `3d70f48`/0.4.0, peat-lite `7a8a8fb`/0.2.5,
peat-gateway `8d16824`/0.1.0, peat-node `4e1b5c8`/0.4.3).

Verdicts: **Verified · Outdated · Misleading · Oversimplified · Wrong · Unverifiable · Speculative-unlabeled**.

| Claim (quote) | Location | Verdict | Evidence | Fix |
|---|---|---|---|---|
| "the SHA-256 of an Ed25519 public key … `PeerId`/`DeviceId` = SHA-256(Ed25519 pubkey) — one derivation, used everywhere." | m9 §001, m9 "Eight facts", m9 §005 | **Wrong/Misleading** | `DeviceId` = **first 16 bytes** of SHA-256(Ed25519 pubkey), not the full digest (peat.md §3; peat-mesh.md §3 `device_id.rs:39-47`). `NodeId` differs per layer: peat-mesh `transport::NodeId` is a bare `String` newtype (peat-mesh.md §3 `transport/mod.rs:107`); peat-btle `NodeId` is a `u32` = first 4 bytes of **BLAKE3**(pubkey) (peat-btle.md §3 `identity.rs:139-148`); peat-lite `NodeId` is a bare `u32` with no key derivation (peat-lite.md §4 `node_id.rs:15`); peat-node uses iroh `EndpointId` (raw Ed25519 pubkey, no SHA-256 wrap) (peat-node.md Identity). "One derivation used everywhere" is false. | State "DeviceId = SHA-256(Ed25519 pubkey) truncated to 16 bytes"; note NodeId is layer-specific (string in mesh, u32/BLAKE3 in btle, bare u32 in lite, iroh EndpointId in node). Drop "one derivation everywhere." |
| "Crypto is FIPS 140-3 (AES-256-GCM / P-256-384 / Ed25519) — newer than prose docs that say ChaCha20 / X25519." | m9 "Eight facts" #8, m9 §005, m3 FIPS note | **Verified (with caveat)** | peat-mesh swapped ChaCha20→AES-256-GCM and X25519→ECDH-P256 at rc.12 (peat-mesh.md §6 `encryption.rs:1-45`, `Cargo.toml:46-52`); peat-btle migrated 2026-05-18 (peat-btle.md §0,§5); peat-lite/peat-gateway/peat-node FIPS-clean. **Caveat unstated:** the `aes-gcm`/`p256` crates are pure-Rust RustCrypto, NOT a CMVP-validated module; peat-btle#75 (migrate to aws-lc-rs) is OPEN (peat-btle.md §0.2). | Add the algorithm-vs-module-validation distinction: FIPS-*approved algorithms*, but not yet inside a CMVP-validated module (peat-btle#75 open). |
| "Crypto is FIPS 140-3 … P-256-384" / "ECDH P-256/384" / "AES-256-GCM, ECDH P-256/384, HKDF/HMAC-SHA-256" | m8 cipher caveat, m9 §005 | **Unverifiable** | Ground truth confirms ECDH on **P-256** only (peat-mesh.md §6 `p256 = 0.13`; peat-btle.md §5 `peer_key.rs`). No P-384 found in any audited source. The "/384" is unsupported. | Drop "/384" unless a spec source is cited; ground truth shows P-256 only. |
| "MLS for forward secrecy" (listed in §005 security spec) | m9 §005 | **Speculative-unlabeled** | No MLS implementation exists: grep for `openmls/mls-rs/rfc 9420/DHKEMP256` empty across peat-protocol + peat-ffi; no `openmls` in Cargo.lock. MLS is Proposed (ADR-044), and ADR-044 carries pre-FIPS ChaCha20 refs (peat.md §6). README presenting MLS as shipped is the recorded high-risk error. | Label MLS **Proposed (ADR-044, not implemented)**; note ADR-044 predates the FIPS rule. |
| "current code uses Cell / Cohort / Federation / Coalition (ADR-066)" with leaf tier named **Platform** | mbig §5, m25 hierarchy table ("Platform (0)") | **Misleading/Outdated** | ADR-066 is **Proposed**, not accepted (peat.md §10). peat-mesh `HierarchyLevel` leaf is named **`Node`** (=0), NOT `Platform` (peat-mesh.md §4 `beacon/types.rs:56-67`). peat-protocol `HierarchyLevel` also uses `Node` (peat.md §4 `authorization.rs:331-343`). peat-btle still uses legacy `Platform/Squad/Platoon/Company` (peat-btle.md §4). The hub's "Platform (0)" leaf matches no shipped enum. | Show leaf as `Node` (0) per peat-mesh code; flag ADR-066 as Proposed and the rename as mid-flight (epics #904/#968); note peat-btle still ships Squad/Platoon/Company. |
| "The authoritative, current tiers — `peat_mesh::beacon::HierarchyLevel` (post-ADR-066)" table lists `Platform (0)` | m25 | **Wrong** | `peat_mesh::beacon::HierarchyLevel` literally is `Node=0, Cell=1, Cohort=2, Federation=3, Coalition=4` (peat-mesh.md §4 `beacon/types.rs:56-67`). The code says `Node`, not `Platform`, at tier 0. | Correct the table's tier-0 row to `Node`. |
| "leader election is deterministic … Every node computes the same score … no vote-counting round … split-brain safe" | m2, m25 step 3, m6 Trace B | **Verified** | peat-mesh `DynamicHierarchyStrategy::determine_role` is deterministic capability scoring, no Raft/Paxos/quorum (peat-mesh.md §5 `dynamic_strategy.rs:190-224`); peat-protocol maintenance uses priority-ordered scoring (peat.md §7 `maintenance.rs:336-388`). | None — accurate. (Optional: note the *exact* weighted-score formula shown is the curriculum/spec model; see next row.) |
| "score = compute·0.30 + communication·0.25 + sensors·0.20 + power·0.15 + reliability·0.10" with file ref `cell/leader_election.rs (~line 78)` | m2, m25 step 3 | **Unverifiable** | The actual shipped scoring (peat-mesh `calculate_leadership_score`) weights mobility, resource availability (1−CPU%, 1−mem%), battery%, ×1.1 can_parent, ×parent_priority (peat-mesh.md §5 `dynamic_strategy.rs:100-138`) — NOT the 0.30/0.25/0.20/0.15/0.10 compute/comms/sensors/power/reliability split. The cited path `cell/leader_election.rs:78` is not corroborated in any ground-truth file; the spec-doc weights (spec 004) differ from peat-mesh's runtime formula. | Label the weighted formula as the **spec-004 model**; show peat-mesh's actual factors (mobility/resource/battery/can_parent) as the shipped behavior, or flag the path as unverified. |
| "heartbeats every 2s … ≥3 missed (~6s) ⇒ re-elect; election 5s" | m25 step 3, m25 lifecycle diagram | **Unverifiable** | No ground-truth file cites 2s heartbeat / 6s / 5s election constants for cell leader election. peat-node has reconnect-watchdog timings (5s/120s) but those are a different mechanism (peat-node.md). These specific election timing constants are not traced to code in any audit. | Cite a source for the 2s/6s/5s constants or mark them as illustrative/spec values, not verified code constants. |
| "Formation handshake … HKDF-SHA-256 … HMAC-SHA-256(nonce ‖ formation_id) … verify in constant time … ALPN `peat/formation-auth/1`" | m25 step 2 | **Verified** | `FORMATION_HANDSHAKE_ALPN = b"peat/formation-auth/1"`, 30s timeout, HMAC-SHA-256 PSK proof, key never on wire, constant-time compare via `subtle` (peat.md §7 `formation_handshake.rs:49`; peat-mesh.md §6 `formation_key.rs:13,34,49`). | None — accurate. |
| "`CellRole`: `Leader` + assigned `Sensor`, `Compute`, `Relay`, `Strike`, `Support`, `Follower`" | m25 step 4 | **Wrong (one variant)** | Actual `CellRole` variants are `Leader, Sensor, Compute, Relay, Strike, Follower` (peat.md §4 `models/role.rs:14-24`). **`Support` is not a variant.** The hub invented `Support`. | Remove `Support`; correct list to Leader/Sensor/Compute/Relay/Strike/Follower. |
| "name the 5 QoS classes" / "P1–P5" / "QoS classifies every message P1–P5" | m2, m1 diagram, m6 | **Verified** | peat-mesh QoS 5-class Critical=1…Bulk=5 (peat-node.md QoS `qos/mod.rs:70-101,154-161`); peat-protocol qos ADR-019 (peat.md §8). | None — accurate (5 classes confirmed). |
| "five RBAC levels" implied; module map says `security` = "RBAC, membership certs" | m2 | **Oversimplified** | Actual `Role` (RBAC) enum has 5 variants `Leader, Member, Observer, Commander, Admin` (peat.md §4 `authorization.rs:50-64`). The hub does not name them (avoids the README's wrong Operator/Supervisor names — good), but also never states them. README's wrong names (Operator/Supervisor) are NOT propagated here. | Optionally name the actual RBAC roles (Leader/Member/Observer/Commander/Admin) and distinguish from `AuthorityLevel` (Observer/Advisor/Supervisor/Commander) and `CellRole`. |
| "Three implementations plug in: Iroh QUIC … peat-lite UDP … and BLE." (pluggable transport) | m3 | **Verified** | peat-mesh ships exactly these three MeshTransport impls: Iroh/QUIC (`iroh_mesh.rs:508`), BLE (`btle.rs:208`), peat-lite UDP bridge (`lite.rs:841`) (peat-mesh.md §1). | None. (Note: `UdpBypassChannel` is a separate primitive, not a 4th transport — hub correctly omits it.) |
| "Cross-transport bridging (ADR-059) goes through a `Translator` trait" | m3, m4 | **Verified (status caveat)** | `Translator` trait + `BleTranslator` shipped in peat-mesh (peat-mesh.md §8 `btle_translator.rs`); peat-btle 0.4.0 dropped the back-edge, exposing only codec (peat-btle.md §1,§7). ADR-059 itself is **Proposed**; Amendment 4 cycle-break tracked by peat#828 (open) (peat.md §1). | Add status: trait shipped in peat-mesh, ADR-059 still Proposed, amendment work open (#828). |
| "FIPS posture (ADR-060): AES-256-GCM (`aes-gcm`), ECDH P-256 (`p256`), `rustls` over `aws-lc-rs`." | m3 FIPS note | **Verified** | peat-mesh uses aes-gcm 0.10, p256 0.13 ecdh, aws-lc-rs TLS not ring (peat-mesh.md §6 `Cargo.toml:46-52,121,136-141`). ADR-060 is Proposed but §5 already implemented (peat.md §5,§10). | Optionally note ADR-060 is formally Proposed though already coded. |
| "negentropy … finds the difference between two doc sets in O(log n) rounds by exchanging fingerprints" | m3 | **Verified (algorithmic claim)** | Negentropy set reconciliation shipped (peat-mesh.md §8 `negentropy_sync.rs`; ADR-040 issue #435); "O(log n) rounds" is a module-doc/algorithmic claim, not independently benchmarked (peat-mesh.md §9). | Note O(log n) is the algorithm's claim, not a measured benchmark. |
| `SyncMessageType` enum: `DeltaSync 0x00, StateSnapshot 0x01, Tombstone 0x04, SyncBatch 0x07, NegentropyInit 0x08, NegentropyResponse 0x09, NegentropyRequest 0x0A` | m3 | **Unverifiable** | Negentropy + Automerge sync over Iroh are confirmed shipped (peat-mesh.md §2,§8), but the specific one-byte tag values for this enum are not enumerated in any ground-truth file. Plausible but uncited. | Verify the exact discriminant values against `storage/automerge_sync.rs` or mark as illustrative. |
| sequence diagram tags `TombstoneBatch 0x05`, `TombstoneAck 0x06` (in mermaid, not in the enum above) | m3 diagram | **Unverifiable/Internal-inconsistency** | The diagram introduces `0x05`/`0x06` tags absent from the `SyncMessageType` enum shown directly above it; neither is in ground truth. | Reconcile the diagram with the enum; cite tag values or remove the specific bytes. |
| "gossip — re-push to all other peers except B (ADR-061)" | m3 diagram | **Unverifiable** | Transitive gossip with echo suppression is confirmed in peat-node (`node.rs:963-999`, peat-node.md), but the **ADR-061** citation is not corroborated by any ground-truth file (the audited ADR set includes 059/060/062/063, not 061). | Confirm ADR-061 exists/applies, or drop the specific ADR number. |
| "peat-mesh … Ships the `peat-mesh-node` binary." / binary `peat-mesh-node` (Module 8 table, Module 3) | m1, m3, m8 | **Unverifiable/Likely-misnamed** | peat-mesh.md §7 documents the broker (feature `broker`) and the consumer entry points but does not confirm a `peat-mesh-node` binary by that name. The actual node binary repo is **peat-node** (peat-node.md), and Module 7 itself flags uncertainty ("confirm vs. the in-repo peat-mesh-node binary"). Binary name unverified. | Verify whether `src/bin/peat-mesh-node.rs` exists at this HEAD; reconcile with peat-node. The hub is internally uncertain (m7 vs m8). |
| `ConnectionState { Healthy (RTT<100ms,0% loss), Degraded (RTT>=100ms OR loss>5%), Suspect, Dead }` | m3 | **Unverifiable** | peat-mesh has `ConnectionHealth`/`DisconnectReason` types (peat-mesh.md §7), and capabilities advertise hardcoded 100 Mbps/10ms defaults (peat-mesh.md §9 `capabilities.rs:150-160`), but the specific RTT<100ms / loss>5% thresholds for these enum variants are not cited in ground truth. | Cite the thresholds against `transport/mod.rs` or mark as illustrative defaults. |
| "peat-lite … bounded CRDT primitives that fit in 256 KB" / table: NodeId 4B, CannedMessage 1B, LwwRegister sizeof(T)+12, GCounter<N=32> ~260B | m1, m4 | **Verified (figures) / Misleading (256KB)** | NodeId 4B (`u32`), CannedMessage 1B (`#[repr(u8)]`), GCounter ~260B at 32 nodes, all confirmed (peat-lite.md §9 `counter.rs:17-18`). BUT "256 KB" is a **design-target/marketing figure**, not enforced or measured — no allocator cap, no static-RAM assertion (peat-lite.md §9 README:7,13). | Keep the per-type sizes (verified). Label "256 KB" as a stated design target, not a measured/enforced bound. |
| "peat-lite … LwwRegister, GCounter" as the primitive set | m4 table | **Verified (with omission)** | Implemented **core** library CRDTs are only `LwwRegister` + `GCounter` (+ canned-message hybrids) (peat-lite.md §3a). `PnCounter` is firmware-only; `OrSet` is a reserved wire byte with no struct anywhere (peat-lite.md §3a,§8). The hub table showing only LwwRegister/GCounter is accurate for the published library. | Accurate; optionally note PnCounter/OrSet exist as wire-type bytes only (PnCounter firmware-only, OrSet unimplemented). |
| "A `Document (0x07)` message type carries the universal `peat_mesh::Document` envelope" | m4, m6 Trace A | **Misleading (namespace)** | The Document envelope and 0x07 message type are **defined in peat-lite** (`protocol/document.rs`, `message_type.rs` Document=0x07), not `peat_mesh::Document` (peat-lite.md §6). It is peat-lite's universal envelope (ADR-059 Amendment 4 universal half). | Refer to it as peat-lite's `Document` envelope (peat-lite `protocol::document`), not `peat_mesh::Document`. |
| "peat-btle … targets <5% and 18–24 h … typical BLE-mesh SDKs burn 20%+ duty cycle (3–4 h watch life)" | m4 | **Unverifiable** | Battery hours (~6/12/20/36h), "18–24h", "3–4h competitor", "<5% duty cycle", "20%+ for commercial SDKs" are all uncited README marketing with no in-repo measurement (peat-btle.md §8). UltraLow profile (36h) has no code path at all (peat-btle.md §7). | Flag these as uncited vendor/marketing estimates, not measured Peat figures. |
| "Security is two-phase: mesh-wide AES-256-GCM (HKDF-derived), then per-peer ECDH-P256 E2EE." | m4 | **Verified** | peat-btle: mesh-wide AES-256-GCM + HKDF-SHA256 (`mesh_key.rs`), per-peer ECDH-P256 + AES-256-GCM E2EE (`peer_key.rs`, `peer_session.rs`) (peat-btle.md §5,§7). | None — accurate and FIPS-current. |
| "peat-btle … One trait, six platforms" then table lists Linux/macOS-iOS/Android/ESP32 (4 rows) | m4 | **Misleading (count)** | Heading says "six platforms"; ground truth: Linux/BlueZ, macOS, iOS (Beta/in-flight), Android, ESP32, Windows (in-flight, untested) (peat-btle.md §1). iOS and Windows are **In flight**, not shipped. The 4-row table omits Windows and conflates macOS/iOS. | Reconcile "six" with the table; mark iOS (Beta) and Windows (untested) as In-flight, not shipped. |
| "BLE … docs are chunked and reassembled (`sync/protocol.rs`: `ChunkHeader` = 8 bytes, `ChunkReassembler`)" / "GATT `0x1826`" (m9) | m4, m9 §001 | **Unverifiable / Wrong UUID** | MTU chunking + reassembly is shipped (peat-btle.md §7 `transport.rs:298-319`), but `ChunkHeader`=8B and path `sync/protocol.rs` are not cited in ground truth. GATT service UUID code-of-record is `a1b2c3d4…`/16-bit `0xA1B2` (peat-btle.md §0.4 `lib.rs:337,343`); README's `0xF47A` and `gatt/mod.rs`'s `f47ac10b…` are stale. **`0x1826` (m9 §001) matches none of these** — `0x1826` is the standard Fitness Machine GATT UUID, not Peat's. | Use `0xA1B2` / `a1b2c3d4…` as the service UUID; the m9 `0x1826` is wrong. Verify ChunkHeader size/path. |
| "peat-lite is a pure leaf — zero Peat dependencies (only `heapless`)" | m1, m4 | **Verified** | peat-lite single runtime dep `heapless` 0.8, no Peat deps (peat-lite.md §1 `Cargo.toml:38`). | None. |
| "peat-btle is almost a leaf — its only Peat dependency is an optional `peat-lite`. It does not depend on peat-mesh…" | m1, m4 | **Verified** | peat-btle 0.4.0 has **zero peat-mesh dependency**; `peat-lite-frame` feature depends on peat-lite 0.2.5 (peat-btle.md §1,§7 `Cargo.toml:47,174`). | None. |
| "peat-mesh pulls in peat-btle (bluetooth) and peat-lite (lite-bridge)" | m1, m4 diagram | **Verified** | peat-mesh `bluetooth`→peat-btle, `lite-bridge`→peat-lite UDP bridge (peat-mesh.md §1; peat.md §1). | None. |
| Dependency graph SVG: `peat-protocol depends on peat-mesh`, `peat-gateway uses peat-mesh as a library`, `peat-mesh → peat-schema` | m1 SVG + legend | **Verified (mostly)** | peat-protocol re-exports peat_mesh + peat_schema (peat.md §0,§8 `lib.rs:105-106`); gateway depends on peat-mesh as library (peat-gateway.md §1). **Minor:** the arrow `peat-mesh → peat-schema` is unverified — peat-schema lives in the peat umbrella; peat-mesh is external and may not depend on peat-schema directly. | Verify peat-mesh→peat-schema edge; peat-schema is a peat-umbrella crate, peat-mesh is the external repo. |
| "Built by Defense Unicorns, Apache-2.0." / repos under github.com/defenseunicorns | m0, m7 | **Verified** | All ground-truth files cite `github.com/defenseunicorns/peat*` repos. Apache-2.0 not contradicted. | None. Note: peat-ffi JNI symbols still being de-ATAK'd (#846) — vendor-name cleanup in flight (peat.md §8). |
| "~95–99% bandwidth reduction via aggregation (validation table: 93–99%)" | mbig §7, m0 (implied) | **Misleading** | Whitepaper validates 79% replication-op reduction at 24 nodes, 60–95% CRDT diff-sync, "95%+" hierarchical target, 86/96% connection-count (peat.md §9 `Whitepaper:21,23,293`). "93–99%" / "95–99%" is a marketing rounding NOT stated verbatim; grep clean in peat-mesh/peat-lite/peat-gateway/peat-node (those repos make no such claim) (peat-mesh.md §9, peat-lite.md §9). | Replace with the whitepaper's actual figures (79% / 60–95% / "95%+" target) and label as single-machine lab/whitepaper, not field. |
| "Mesh is O(n²); hierarchy is O(n log n)." / "1,000 nodes: mesh ≈ 500,000 connections, hierarchy (depth 4) ≈ 4,000" | mbig §1,§3, m6 | **Oversimplified (analytical, not measured)** | Structurally supported by whitepaper connection table (full mesh O(n²) ~9,120 vs hierarchical ~384 at scale; peat.md §9 `Whitepaper:291-293`). It is an analytical/design claim, not a runtime benchmark. The "≈4,000" depth-4 figure is illustrative, not from code. | Label as analytical/design complexity (whitepaper), not a measured runtime result. |
| "Phases: 2-node <1s/100% · 12-node 26s · 24-node 54s (6.1s mean) · simulated 1,000+ confirms O(n log n)." / "The protocol that works for 5 works for 1,000+." | mbig §7, m6 | **Misleading (1,000+)** | Whitepaper validated single-machine to **1000 nodes with a 1023 hard limit** (Linux bridge) — a simulation, not field deployment; open epics #724-727 still target 900/1.2K/10K (peat.md §9 `Whitepaper:13,163,213`). "1,000+" is the *current ceiling*, not exceeded. Specific 12-node 26s / 24-node 54s figures not separately corroborated in code-side audits (whitepaper-only). | State "single-machine simulation to ~1000 nodes (1023 hard cap); larger scale is open work #724-727." Avoid "1,000+" implying field-proven. |
| "Maturity: TRL 4–5 (lab-validated, integration-demonstrated)" | mbig §7 | **Unverifiable** | No ground-truth file asserts a TRL level; this is a whitepaper/positioning claim. | Mark as a whitepaper positioning claim, not a code-verifiable fact. |
| "peat-gateway … Observes and manages; never in the data path." / "implements zero wire transports" | m5, m6 | **Verified** | Gateway is control plane only, no mesh transports, registers external `MeshBrokerState` handles (peat-gateway.md §1,§2 `formations.rs:28-30`). | None — accurate. |
| "roles → mesh tiers: Authority→Enterprise, Infrastructure→Regional, Endpoint→Tactical" | m5 | **Verified (with omission)** | Exact mapping confirmed (peat-gateway.md §5 `models.rs:179-188`). Gateway maps only **3 of 4** mesh tiers — `Edge` unreachable (peat-gateway.md §5,§12). Neither set is the ADR-066 vocabulary. | Accurate; add that the 4th mesh tier `Edge` is unreachable from the gateway, and that these are trust/distribution tiers, NOT the ADR-066 hierarchy. |
| "CDC … fans CdcEvents out per-org to sinks: NATS JetStream, webhooks, Kafka, Redis Streams (at-least-once delivery)." + diagram showing all four | m5, m5 diagram | **Wrong (Kafka/Redis)** | Shipped sinks: NATS JetStream, Webhook (both default). **Kafka is a TODO** — dispatcher branch is empty (`engine.rs:78-80`); **Redis Streams does not exist** — no `CdcSinkType::Redis` variant, no code (peat-gateway.md §2,§12 `models.rs:79-84`). The README over-advertises both; the hub propagates the overclaim. | Mark Kafka **In flight (TODO, does not deliver)** and Redis **Proposed/absent**. Show only NATS + Webhook as shipped in the diagram. |
| "Identity: OIDC (`openidconnect`) / SAML / CAC federation" | m5 grid | **Misleading** | Only **OIDC token introspection (RFC 7662)** is shipped (peat-gateway.md §2 `enroll.rs:305-392`). SAML and CAC are not corroborated as implemented in any ground-truth evidence. | Mark SAML/CAC as Proposed/unverified; OIDC is the shipped federation path. |
| "Four backends: PlaintextProvider, LocalKeyProvider (AES-256-GCM), AwsKmsProvider, VaultTransitProvider." | m5 | **Verified** | Exactly these four KeyProviders; selection priority KMS→Vault→local→plaintext; PENV v1 envelope (peat-gateway.md §6,§7 `crypto/mod.rs`). | None — accurate. Optionally note local-KEK path uses non-CMVP software AES; KMS/Vault for a FIPS boundary. |
| "Depends on `peat-mesh = "=0.9.0-rc.1"`" | m5 | **Verified (but stale pin)** | Pin is exactly `=0.9.0-rc.1` (peat-gateway.md header `Cargo.toml:23`), intentionally stale per SKILL.md; ecosystem is at rc.42 — gateway lags ~40 RCs. | Accurate; optionally note the pin is intentionally frozen and lags the mesh significantly. |
| Module 5 omits: ingress AuthZ is a permissive stub; no proof-of-possession at enrollment; no NATS broker auth/TLS | m5 | **Misleading (by omission)** | Ingress AuthZ is a PERMISSIVE STUB (all events allowed, peat-gateway#99 open); enrollment validates pubkey length only, no PoP; NATS nkeys/JWT/TLS not implemented (#124/#125) (peat-gateway.md §9,§13). For an air-gap/enterprise reader these are material security gaps. | Add a "not production-secure yet" note: ingress AuthZ stub, no enrollment PoP, no NATS broker auth/TLS. |
| Trace A: ESP32 sensor → peat-lite 16B frame → bridge into DocumentStore → AutomergeBackend → Iroh sync → TAK CoT | m6 | **Verified (path plausibility)** | Each hop maps to shipped code: peat-lite 16B header (peat-lite.md §6), lite-bridge into peat-mesh (`transport/lite.rs`, peat-mesh.md §1), Automerge over Iroh (peat-mesh.md §2,§8), TAK/CoT bridge in peat-transport (peat.md §1). | None — the end-to-end path is real; keep "negentropy → delta" qualifier honest (negentropy is the reconciliation, Automerge carries deltas). |
| "QoS: a P1 contact report preempts P3/P4 traffic at every congested hop." / "<5 s P1 latency" implied | m6, m6 Trace A | **Misleading (enforcement)** | QoS framework + classes shipped (peat-mesh.md §8), and peat-node has QoS-priority relay fanout (peat-node.md). BUT cross-class wire-level **preemption is NOT enforced in v1** (peat-node proto: AttachmentPriority does not pause in-flight BULK, peat-node.md v1 caveats); peat.md §9 marks "<5s P1" a target, QoS enforcement in-flight. | Soften to "QoS prioritizes/orders by class"; note cross-class preemption is not fully enforced (in-flight), and "<5s P1" is a target. |
| "the universal Document envelope" / "TombstoneBatch" etc. — m9 §002 "Deletes are tombstones (7-day retention)" | m9 §002, m8 | **Verified (default)** | peat-node tombstone defaults 168h/7-day, 300s GC interval (peat-node.md `node.rs:64-71`); peat-lite OTA/Document tombstone flag shipped. 7-day retention matches the default. | None — accurate. (Tombstone GC #857-class work is in-flight per prompt, but the 7-day default is real.) |
| "peat-node … Node distribution referenced in Peat URLs — confirm vs. the in-repo peat-mesh-node binary" (treated as NOT-local / uncertain) | m7 | **Outdated/Wrong** | peat-node is a real, separate, actively developed repo (`defenseunicorns/peat-node` @ 0.4.3, peat-node.md) — a gRPC/Connect sidecar embedding peat-mesh + peat-protocol, NOT a duplicate of a "peat-mesh-node" binary. It supports Iroh QUIC only (no BLE despite a DESIGN.md diagram label). | Treat peat-node as a confirmed standalone sidecar repo; describe its gRPC/Connect API (28 RPCs defined / 27 impl). Stop framing it as "confirm vs peat-mesh-node." |
| "peat-inference is real, just not cloned here … YOLOv8, ByteTrack, GStreamer, TensorRT" | m7 | **Unverifiable** | No ground-truth file covers peat-inference; sourced only from DEVELOPER_GUIDE. Cannot confirm against code in this audit set. | Mark as documented-but-unverified (no code audited); the YOLOv8/TensorRT specifics are doc claims. |
| "peat-registry … UDS registry replication for DDIL (ADR-054)" | m7 | **Unverifiable** | ADR-054 not in the audited ADR set; peat-mesh `certificate.rs` comments reference "peat-registry artifact routing" (peat-gateway.md §5) but no registry repo audited. | Mark ADR-054/peat-registry as referenced-but-unaudited. |
| Quickstart: "`cargo build -p peat-quickstart --release` … needs Rust 1.70+ and protoc" / binary `peat-quickstart` | m8 | **Unverifiable** | No ground-truth file confirms a `peat-quickstart` crate/binary or that the quickstart builds at the audited HEAD. peat-mesh README quick-start uses `PeatMeshBuilder` and may be aspirational (peat-mesh.md §7). MSRV 1.70 matches peat-lite (peat-lite.md). Build-command verification was explicitly deferred (peat.md §13). | Verify `peat-quickstart` exists and builds at this HEAD before presenting as a hands-on path; commands are currently unverified. |
| "set `PEAT_CONNECTION_RECYCLE_SECS=0` … 60s connection-recycle … ~5s sync outages each minute, #435/#892" | m8 | **Unverifiable** | #435 is the negentropy/set-reconciliation issue (peat.md §2,§11), not a connection-recycle bug; #892 not in the audited issue list. The 60s-recycle/5s-outage behavior is not corroborated in ground truth. | Verify the env var, the 60s/5s behavior, and the issue numbers; #435 appears mis-cited. |
| Ports: "4040 P2P, 8080 HTTP, 5353 mDNS"; env vars `PEAT_BIND_PORT (4040)`, `PEAT_HTTP_PORT (8080)`, `PEAT_CELL_SIZE (5)` | m8 | **Unverifiable** | peat-lite uses `DEFAULT_PORT=5555`, multicast 239.255.72.76 (peat-lite.md §6) — different from these. peat-protocol `DEFAULT_CELL_SIZE=5` matches (peat.md / lib.rs defaults). 5353 is standard mDNS. 4040/8080 not corroborated in any audit. | Verify port defaults against the quickstart/operator config; note peat-lite's own UDP default is 5555. |
| "the operator guide names ChaCha20-Poly1305 — that's pre-FIPS; the spec + ADR-060 (2026-05-18) moved to AES-256-GCM + ECDH P-256/384" | m8 | **Verified (good flag) / P-384 unverifiable** | Correctly flags the stale ChaCha20 doc and the FIPS swap (peat-mesh.md §6 dated 2026-05-18; peat.md §5). **"P-256/384"** — only P-256 is in code; P-384 unverified. | Keep the ChaCha20→AES-GCM flag (good). Drop "/384". |
| "Prometheus: `peat_peers_connected`, `peat_cell_size`, `peat_sync_latency_seconds`, `peat_leader_elections_total`" / endpoints `/api/v1/{status,peers,cell,network/partitions}` | m8 | **Unverifiable** | peat-gateway exposes `/health`, `/metrics` (Prometheus) (peat-gateway.md §8), but these specific metric names and `/api/v1/...` endpoints are not enumerated in any ground-truth file. | Verify metric/endpoint names against the operator guide / code, or mark as illustrative. |
| "wiping a node's data dir is safe — it rejoins and re-syncs from peers (CRDT convergence)" | m8 | **Verified (conceptual)** | CRDT convergence + peer re-sync is the design (Automerge + negentropy, peat-mesh.md §2,§8; peat-node "rejoins and re-syncs"). | None — conceptually sound. |
| m9 §001: "`PeerId = SHA-256(Ed25519 pubkey)`; big-endian." / "Peat-Lite (`0xCAFE`, CRC-16)" | m9 §001 | **Wrong** | (a) DeviceId is SHA-256 truncated to 16 bytes (see first row). (b) peat-lite wire magic is **`"Peat"`** (4 ASCII bytes), NOT `0xCAFE`, and the header has no CRC-16 — it is magic+version+type+flags+node_id+seq (peat-lite.md §6 `header.rs`, constants.rs:4). `0xCAFE`/CRC-16 are invented. | Correct peat-lite framing to magic `"Peat"`, 16-byte header, no CRC-16; fix the SHA-256 truncation. |
| m9 §002: "Map=LWW, List=RGA, Counter=PN" | m9 §002 | **Unverifiable/Oversimplified** | Automerge's CRDT semantics are RGA for lists/text and observed-remove maps; peat-mesh.md §2 says "RGA/observed-remove text & maps," not "Map=LWW." Automerge maps are last-write-wins per key, so "Map=LWW" is loosely defensible, but "Counter=PN" maps to Automerge's counter type — sourced from spec 002, unverified against code here. | Reconcile with Automerge's actual semantics (observed-remove maps, RGA lists, Automerge Counter); cite spec 002 as the source. |
| m9 §005: "challenge-response with version negotiation (ADR-065)" | m9 §005 | **Unverifiable** | ADR-065 not in the audited ADR set (audited: 011/032/035/039/041/046/051/052/059/060/063/066). Cannot confirm. | Verify ADR-065 exists/applies or remove the citation. |
| "ADR archive … ~60 decision records" / "ADR-007/011 (CRDT backend)" | m7 | **Partially-verified** | peat.md §10 confirms ADR-011 (CRDT+Networking, Proposed). peat#695 triages "22 Proposed ADRs," consistent with a large archive (peat.md §11). "~60" not precisely confirmed; ADR-007 not in audited set. | "~60" is plausible but uncited; ADR-011 confirmed Proposed (so "CRDT backend" is a Proposed decision, worth flagging). |
| "Authority is replicated CRDT state … six-level authority model (Level 0 Root … Level 5 Node)" | mbig §5 | **Misleading/Outdated** | The 6-level Root/Cluster/Formation/Group/Team/Node ladder is whitepaper vocabulary; the hub itself flags it as older. The shipped `AuthorityLevel` enum is `UNSPECIFIED/OBSERVER/ADVISOR/SUPERVISOR/COMMANDER` (peat.md §4 `node.proto:61-67`) — a different axis with different names/count. "Authority replicated as CRDT state" is a design framing, not traced to a specific shipped mechanism. | Flag the 6-level model as whitepaper-only; the shipped authority axis is `AuthorityLevel` (5 values). Distinguish from HierarchyLevel and RBAC Role. |
| External script: `<script src="https://cdnjs.cloudflare.com/.../mermaid.min.js">` | bottom of file | **Misleading (self-containment) / will break offline** | The prompt §8 requires the HTML be self-contained (inline SVG/CSS, no external assets/CDNs). The hub loads Mermaid from a CDN — diagrams will not render in an air-gapped/offline environment (the target audience). | Inline Mermaid or pre-render diagrams to inline SVG; remove the CDN dependency. |
| "every interactive element's coordinates and labels must match its diagram" — SVG arrow comments contradict drawn paths (e.g. `transport->ffi? actually transport->protocol`) | m1 SVG | **Misleading (diagram integrity)** | The SVG source has contradictory inline comments (e.g. "gateway -> protocol? no, gateway->mesh", "transport->ffi? actually transport->protocol") indicating the drawn arrows may not match intended dependencies. The gateway→mesh long arrow is drawn but the straight `M385,64 L385,150` points gateway→protocol. | Audit each SVG arrow against the manifests; gateway depends on peat-mesh (not peat-protocol per the legend) — make arrows match the legend. |
